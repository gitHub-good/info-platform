# 🧪 测试报告 · 信息整合与 AI 分析平台 M6（体检修复 + 产品能力补齐全量回归）

> 测试编号：TEST-20260924-M6｜ 测试人：测试工程师（虚拟团队）｜ 日期：2026-09-24
> 提测范围：系统体检报告（`docs/02-设计/系统体检-20260924-交互与业务优化清单.md`）P0×3 + P1×8 + P2 择要 + 后端小项×4（各批已自测，本轮集成级验收）
> 代码基线：main @ `f12e6c2`（工作区仅 untracked `backend/.gitignore`，与本轮无关）
> 服务基线：8080 后端 HEAD 进程（V16/V17 已迁移）+ 5173 前端 dev（Vite HMR）全程未重启，所有接口级用例在真实运行服务上执行

## 1. 结论先行

| 维度 | 结果 |
| --- | --- |
| 自动化回归 | 后端 **1085/1085**（0 失败 0 错误 0 跳过，128 报告文件，与自测基线一致）——**但完整 `mvn verify` 门禁因 spotless 格式违规失败**（D1，4 文件，跳过格式检查后复跑测试全绿）；前端 vitest **229/229**（25 文件 4.68s）、oxlint **0 error**（25 warnings 为体检 P3 已登记的 load-in-effect 存量族）、`npm run build` 通过（454.08 kB，gzip 133.68 kB） |
| P0 端到端 | **3/3 通过**：详情页真实接口链路（by-code 解析 3.5ms → detail 262ms 冷 / 5ms 热，三态分区 + 真实公告 + 事件分区，前端 mock 已删）；refresh 令牌全场景拒绝（401/1003）；政策倾向重试上限（V16 落库 + attempts<3 过滤 + 退避窗 + 预算护栏 EXHAUSTED 实证） |
| P1 端到端 | **8/8 通过**：SSE 推送全链路（真实补推：anomaly 帧 + `:heartbeat` 心跳帧 + history + push_record 落库）；搜索中英混合 2ms 级 + 自选五列真实行情；订阅四类型 CRUD + 幂等 + 软退订复活；概览两段式数据齐（llmToday 口径复核一致）；feed 1.12s→270ms→**8ms**；简报 FAILED 重试受理回置 PENDING + DONE 幂等 0 成本直返；前端快修六项源码/测试证据落定 |
| 缺陷 | **P2 × 1**（spotless 门禁红 D1）+ **P3 × 2**（llmToday 口径歧义 D2 / ai_brief SUCCESS 留痕与 FAILED 终态不一致 D3-挂起）；无 P0/P1。遗留风险 1 条（QUOTE 源 errors24h 持续恶化 R1，P2 未修项） |
| Go/No-Go | **Go（附条件）**——P0/P1 全部验收通过且无新增 P0/P1；条件①：D1 spotless 违规 `mvn spotless:apply` + 完整 `verify` 重跑通过后方可发布（预计 <0.5h，纯格式不改行为） |
| 数据治理 | 测前 `VACUUM INTO` 备份（integrity ok）→ 测后精准清理 5 表 9 行测试数据 → 7 表行数与备份逐一 MATCH + `PRAGMA integrity_check` ok（§6） |

一句话：体检 11 条主项（P0×3 + P1×8）**全部在真实运行服务上端到端实证修复达成**，四条后端小项（扫描有界/并发幂等/SSE 心跳/保留期）顺带实证；唯一开单缺陷是工程规范层面（提交代码未过格式门禁），不影响任何运行时行为。

## 2. 范围与环境

### 2.1 测试范围（体检条目 → 用例来源）

| 体检条目 | 交付要点 | 验收方式 |
| --- | --- | --- |
| P0-1 详情页真实接口 | 删前端 mock；by-code 端点；三态分区 | 接口级 E2E + 源码核对 + 组件测试 |
| P0-2 refresh 令牌 | filter 类型校验 401 | 接口级四场景 |
| P0-3 政策倾向重试上限 | V16 + attempts≥3 停扫 + 预算口径 | 迁移/代码/单测/数据分布四侧 |
| P1-4 通知中心 | SSE 消费/铃铛/历史补拉/心跳 | 真实 SSE 流捕获 + 注入推送全链路 + 组件测试 |
| P1-5 搜索 + 自选五列 | search 端点（池 45）+ quotes 批量 + 选择器 | 接口级多场景 + 组件测试 |
| P1-6 订阅管理页 | 四类型 CRUD + Feed 空态 CTA | 接口级 CRUD/幂等/软退订 + 页面源码/测试 |
| P1-7 概览两段式 | 今日推荐/异动/政策 + 平台健康折叠 | overview 数据核验 + 口径 SQL 复核 + 页面源码/测试 |
| P1-8 feed 毫秒级 | recommendationPending + 负缓存 | 接口级三连延迟 + 订阅命中内容验证 |
| P1-9 简报 FAILED 重试 | 重试受理 + 预算后置 | 真实 FAILED 任务重触发 + DONE 幂等对照 + 单测 |
| P1-10 前端快修 | 徽章/移除确认/翻页保留/重试/TaskCenter/下拉缓存 | 源码逐项 + 配套组件测试 |
| 后端小项 | push 有界化 / ai_brief 并发幂等 / SSE 心跳 / 保留期 | 源码 + SSE 实测帧 + 单测 |

不在范围：M1~M5 已交付功能回归（由 1085+229 自动化承接）；体检 P2/P3 未列入本批项（§5 状态对照）。

### 2.2 环境与方法

- 后端：Java 21 + Spring Boot 3 + SQLite（WAL）+ Flyway V16/V17 已迁移（`flyway_schema_history` 核对）；admin/admin123；JWT/CONFIG secret 沿用。
- 前端验证方式（如实标注）：本会话无浏览器自动化工具，前端交互以**组件/页面测试（vitest 229 条，覆盖通知铃铛 8 条/订阅页/概览/选择器/详情/自选等 25 文件）+ 生产构建产物 + 关键交互源码核对**为依据；未做真实浏览器点检（遗留 R2 建议）。
- 数据治理红线：测前 `sqlite3 … "VACUUM INTO '/tmp/m6-regression-backup-20260922.db'"`（integrity ok，未裸拷贝）；测后精准 DELETE 注入数据并逐表核对（§6）。
- 真实 LLM：P1-9 验证中经异步链路产生 2 次真实 SUCCESS 调用（scene 1，成本微小，llm_call_log 曾留痕 id 126/127，测后随基线清理）；预算未做任何真实调整（未改 llm.global）。

## 3. 用例矩阵与结果

### 3.1 自动化回归（金字塔底座）

| 套件 | 结果 | 说明 |
| --- | --- | --- |
| 后端 `mvn -q verify` | **spotless-check 阶段失败**（D1：`AggregationServiceTest.java`[test]、`AggregationService.java`、`SubjectRepositoryImpl.java`、`SubjectController.java` 共 4 文件格式违规，测试阶段未执行到） | **复跑 `mvn -q verify -Dspotless.check.skip=true`：1085/1085 通过（0F/0E/0S，128 surefire 报告）**，与各批自测口径一致 |
| 前端 `npm run test` | **229/229**（25 文件，4.68s） | M6 增量含 NotificationBell 8 / Subscriptions / SubjectPicker / Overview 两段式 / SourceStatusBadge+BriefStatusBadge 语义色 / Watchlist 错误重试等 |
| 前端 oxlint / build | **0 error**（25 warnings）/ 构建通过 | warnings 为体检 P3 已登记 load-in-effect 存量族，0 error 门禁过，口径同 M4/M5 |

### 3.2 P0 端到端（真实服务接口级）

**P0-1 详情页真实接口链路 —— 通过**

| 用例 | 结果 | 证据 |
| --- | --- | --- |
| mock 层移除 | 通过 | `src/api/subject.ts` 无 mock 分支，注释明示「mock 能力由后端 datasource mode=MOCK 提供」 |
| by-code 解析正向 | 通过 | `GET /subjects/by-code/SH600519` 200/3.5ms，返回 id=1 + 名称/市场/行业（内部代码→主键解析） |
| by-code 不存在 | 通过 | `NOTEXIST999` → 404/code 30001「标的不存在」，契约对齐 |
| detail 三态分区 | 通过 | id=1 冷 262ms：quote=ok（茅台 1238.68/-1.0% 真实行情）/ valuation=ok（peTtm 17.39）/ announce=ok（**真实公告 art_code AN202608142827994407，pdf.dfcfw.com 域**）/ finance/news/policy=missing 分区如实用 missing 态；events 1 条真实 PRICE_CHANGE（T08 事件分区） |
| 负缓存（quote 降级场景） | 通过（附注） | 同标的冷 262ms → 热 4~5ms（id=1/id=2 双样本）；**本时窗 push2 实际可达（quote=ok 有真实数据），「受限降级不空白」路径未自然触发**——以 FAILED/MISSING 源的负缓存行为（finance/news missing 分区毫秒返回）与既有单测为补充依据，如实标注 |
| 前端消费 | 通过 | SubjectDetail.test.tsx 在测；构建产物含 by-code 调用链 |

**P0-2 refresh 令牌类型校验 —— 通过（四场景）**

| 用例 | 结果 | 证据 |
| --- | --- | --- |
| refresh 令牌 → 受保护 API | 通过 | `/api/v1/watchlists` → **401 + code 1003「令牌类型不符，须为访问令牌」**（7 天 refresh 不再架空 1h access 设计） |
| access 令牌 → 受保护 API（对照） | 通过 | 200 |
| 无令牌（对照） | 通过 | 401/1003「未提供认证令牌」 |
| refresh 端点双向 | 通过 | refresh 令牌换发 200 正常；**access 令牌冒充 refresh → 401/1003「需 refresh 令牌」** |

**P0-3 政策倾向重试上限 —— 通过（四侧证据）**

| 侧 | 证据 |
| --- | --- |
| 迁移 | V16 `tendency_attempts NOT NULL DEFAULT 0` + `tendency_last_attempt_at` 已落库（flyway history 核对） |
| 代码 | `PolicyTendencyJob`：`policy.tendency.max-attempts`（默认 3，非正兜底 `DEFAULT_MAX_ATTEMPTS=3`）+ `retry-backoff-hours`（默认 2h）扫描前过滤；判断未成功 `recordTendencyAttempt` 原子自增 |
| 单测 | `PolicyTendencyJobTest` 10 用例：attempts=3 断言 / 退避截止 now-2h±10s / 非正配置兜底 / 失败留痕计数源 |
| 数据 | `policy_item`：UNJUDGED 9 条（attempts=0×5 + attempts=1×4），最后尝试 2026-09-24T01:03Z 后零新增（退避窗内符合预期）；今日 scene3 LLM 调用 12 次（对比体检时无限重试失控态）；**概览预算护栏 EXHAUSTED 状态生效**（「系统调用纳入预算口径」修复项工作） |
| 口径复核 | llmToday=101288/20000 经 SQL 复核一致（本地时区日界 09-23T16:00Z 起 + SUCCESS + 剔除探活 scene）——**非缺陷**（但见 D2 口径歧义） |

附注：无 attempts≥3 实例可自然观察（4 条 attempts=1 均在 2h 退避窗内），「满 3 停扫」终态以单测覆盖为据——列入 R3 次日数据点检。

### 3.3 P1 端到端（真实服务接口级 + 前端证据）

**P1-4 通知中心 —— 通过（真实推送全链路）**

| 用例 | 结果 | 证据 |
| --- | --- | --- |
| SSE 建连鉴权 | 通过 | `GET /notifications/stream?access_token=…`（query 鉴权对齐方案 §4.1）流式响应 |
| 真实推送（补推路径） | 通过 | 注入 anomaly_event(pushed=0) → **PUSH_RETRY 30s 内拾取** → push_record 落库（status 推送成功）→ SSE 流捕获 `event:anomaly` 帧（含 subjectCode SH600519 解析 + content）→ history 端点返回同记录 |
| **SSE 心跳帧** | 通过 | 流内捕获 **`:heartbeat`** 注释帧（后端小项实证） |
| history 补拉 | 通过 | `GET /notifications?limit=5` 分页结构（items/nextCursor）正常 |
| 手动跑异动检测 | 通过 | `POST /jobs/ANOMALY_DETECT/run` 202/executionId 5807 → SUCCESS 61ms（当日无新异动不重复告警=去重正确） |
| 前端消费 | 通过 | NotificationBell.test.tsx **8 用例**：建连 token 走 query / 断线指数退避重连 / 重连后补拉 history / **心跳帧不触发 onmessage 语义** / 未读徽章水位 / 全部已读 |

附注：SSE 实时帧经补推路径（`recoverPendingAnomalies → processAnomaly` 全链路含 SSE 直推）验证；「Job 即时发事件→SSE」入口因当日去重未自然触发，行为等价（同一推送出口）。

**P1-5 标的搜索 + 自选五列行情 —— 通过**

| 用例 | 结果 | 证据 |
| --- | --- | --- |
| 中文名精确 | 通过 | `q=茅台` → SH600519 贵州茅台，2.6ms |
| 名称片段 | 通过 | `q=招商` → SH600036 招商银行，2.4ms |
| 代码模糊 | 通过 | `q=600036` → SH600036，2.3ms |
| 行业词多命中 | 通过 | `q=银行` → 平安/招商/工商/农业 4 只，2.3ms |
| 空结果 | 通过 | `q=zzzz` → 空数组 200 不报错，1.9ms |
| quotes 批量（五列数据源） | 通过 | `GET /subjects/quotes?ids=1,2` 两行全返回：茅台 1238.88/-0.99% + 平安 11.39/+0.35%（真实行情），摘要字段（代码/名称/行业/市场）齐 |
| 选择器交互 | 通过 | SubjectPicker.test.tsx（防抖 300ms/键盘↑↓Enter/三态兜底） |
| 池容量 | 通过 | subject_master 45 只（V17 幂等种子） |

勘误记录：首轮误用参数名 `keyword` + 中文未编码得 400，属**调用方错误**（契约为 `q`，前端 axios 自动编码），非缺陷。

**P1-6 订阅管理页 —— 通过**

| 用例 | 结果 | 证据 |
| --- | --- | --- |
| 四类型创建 | 通过 | THEME(1)「白酒」/ SUBJECT(2) id=1 / EVENT(3) PRICE_CHANGE / POLICY(4)「人工智能」全 200 |
| 重复创建幂等 | 通过 | 同 type+key 再 POST → **返回原 id 不新建行**（UNIQUE 约束应用层复用） |
| 软退订 | 通过 | DELETE id=3 → 200，DB status 1→0（记录保留） |
| 重新订阅复活 | 通过 | 再 POST 同 type+key → 同 id=3 status 回 1（不新建） |
| 越权/不存在 | 通过 | DELETE id=999 → 404/30050「订阅不存在」 |
| 前端页面 | 通过 | Subscriptions.tsx + Subscriptions.test.tsx（CRUD/类型徽章/退订二次确认/软退订重订阅） |
| Feed 空态 CTA | 通过 | Feed.tsx 空态引导 `#/subscriptions`（体检 P1-3 修正指向实证） |

**P1-7 概览两段式 —— 通过**

| 用例 | 结果 | 证据 |
| --- | --- | --- |
| overview 数据完整性 | 通过 | llmToday（tokenUsed/costMicros/budget/status 状态机 EXHAUSTED）/ anomalyToday=1（与注入异动联动）/ policy24h=8+latest 5 条标题 / jobHealth 5750 runs 0 failed unhealthy=[] / sourceHealth 7 源全量（mode/lastEventType/errors24h） |
| llmToday 口径复核 | 通过（非缺陷） | SQL 按同窗口复算 101288 完全一致（本地时区日界+SUCCESS+剔探活）；EXHAUSTED 为护栏生效表现 |
| 推荐卡状态机 | 通过 | feed 联动实证：recommendationPending=false + feed 首条 type=recommendation（个性化评分明细：已读热度/标的订阅/主题命中加权）——「今日推荐」有真实消费数据 |
| 两段布局 | 通过 | Overview.tsx 源码：上半「今日」区（推荐卡 lg 占 2 列 + 异动/最新政策右列）+ 下半「平台健康」三卡收纳 + 异动卡行情源异常警示条（E2 联动）；Overview.test.tsx 在测 |

**P1-8 feed 毫秒级 —— 通过**

| 用例 | 结果 | 证据 |
| --- | --- | --- |
| 延迟三连 | 通过 | 1.12s（首访含内容合成）→ 270ms → **8ms**（<2s 达标，缓存生效） |
| recommendationPending | 通过 | FeedListView 增量字段在响应（ADR-0025）；本时窗 false（推荐已就绪） |
| 订阅命中入流 | 通过 | feed 5 条：推荐 + 政策（命中「白酒」主题订阅）+ 公告（标的订阅）——个性化闭环真实联动 |
| 负缓存 | 通过 | 与 P0-1 同机制（FAILED/MISSING 源毫秒返回 missing 分区不阻断） |

**P1-9 简报 FAILED 可重试 + 预算后置 —— 通过**

| 用例 | 结果 | 证据 |
| --- | --- | --- |
| 真实 FAILED 重试受理 | 通过 | 任务 id=7（异步生成失败转 FAILED 的**真实样本**）→ 再 POST → **202 返回同 taskId=7** + DB status 2→**0 回置 PENDING** + version 自增——「FAILED 占坑当日幂等键」缺陷修复实证 |
| 重试后异步再生成 | 通过（附注 D3） | 异步链路真实执行（llm_call_log 2 行 scene1 SUCCESS），终态仍 FAILED（见 D3 观察） |
| DONE 幂等 0 成本直返 | 通过 | 伪造当日 DONE 记录（幂等键 `1:1:20260924` 冒号格式）→ POST → 同 taskId 202 直返、version/status 不变——**「先幂等查重后预算预检」**（预算 EXHAUSTED 状态下未拦截当日已生成结果的读取）实证 |
| 并发幂等（后端小项） | 通过 | `DuplicateKeyException` 仓储译码→重查命中返回同任务（AIBriefService 140-141/203-217 + 单测） |
| 单测覆盖 | 通过 | AIBriefServiceTest:370「预算耗尽 + 当日 FAILED——重试仍须预算闸门拦截」等 |

过程记录（如实）：伪造 FAILED 首版幂等键误用连字符格式未命中（系统为冒号 `subjectId:briefType:yyyyMMdd`），意外产生真实 FAILED 任务样本 id=7，反而使重试验证走的是**全真实路径**；伪造行与产物已随基线清理。

**P1-10 前端快修抽验 —— 通过（六项）**

| 项 | 证据 |
| --- | --- |
| 徽章语义色 | BriefStatusBadge：处理中 amber/已完成 emerald/失败 rose/待核实 muted；SourceStatusBadge：ok emerald/missing muted/failed+timeout rose（对齐 JobLog STATUS_META className 方案）；两组件均有配套测试 |
| Watchlist 移除确认 | WatchlistDetail.tsx:228 Dialog「确认移除该标的？」（destructive 二次确认规范落位） |
| Policy/JobLog 翻页失败保留 | 两页 `loadMoreError` 独立状态（Policy:46/JobLog:121），错误条不替换列表 |
| 错误态重试 | SubjectDetail:129、Watchlist:251 重试按钮 + Watchlist.test:401「失败→重试→恢复」用例 |
| TaskCenter | RESTART amber「重启后生效」常驻（:129/:148）+ 编辑保存失败保持 Dialog（组件测试覆盖） |
| 下拉缓存 | Policy:63/JobLog:138「无过滤全量加载时缓存全集，过滤态下拉不收缩」 |

### 3.4 后端小项四条

| 项 | 证据 | 结果 |
| --- | --- | --- |
| push 扫描有界化 | PushRetryJob `scan-limit` 默认 100 LIMIT + `pending-retention-days` 7 天过期截止 + 空转轮零日志 | 通过（源码+单测） |
| ai_brief 并发幂等 | 见 P1-9 | 通过 |
| SSE 心跳 | 实测 `:heartbeat` 帧 | 通过 |
| findPending 保留期 | 同 push retention 7 天 | 通过（源码） |

## 4. 缺陷清单

| # | 级别 | 标题 | 复现/证据 | 期望 | 状态 |
| --- | --- | --- | --- | --- | --- |
| D1 | **P2** | 提交代码 spotless 格式违规致 `mvn verify` 门禁失败 | `mvn -q verify` 在 spotless-check 阶段退出 1，违规 4 文件：`AggregationServiceTest.java`（test）、`AggregationService.java`、`SubjectRepositoryImpl.java`、`SubjectController.java`（均为 P0-1 批次触碰文件）；skip 后测试本体 1085/1085 全绿 | `mvn spotless:apply` 后完整 verify 通过再提测 | **新建**（回开发，<0.5h 修复 + 重跑 verify） |
| D2 | P3 | 概览 llmToday 口径歧义：全用户 SUCCESS 合计对比当前用户个人预算 | admin 个人用量 3285/20000 未超、简报可正常生成，但概览显示 101288/20000 EXHAUSTED（系统 userId=0 调用计入分子） | 口径澄清：分子按 per-user 或明示「平台总消耗」标签（歧义单回 PM/架构） | 新建（登记澄清） |
| D3 | P3 | ai_brief 任务两次 LLM SUCCESS 留痕后终态仍 FAILED（content null），留痕与终态不一致 | 任务 id=7 重试：llm_call_log id 126/127 scene1 SUCCESS（02:39Z），终态 status=2 | SUCCESS 调用后应产出内容或失败原因可见；无法在本窗口复现深究 | **挂起留档**（复现步骤缺口：需抓 LLM 响应体；建议开发在解析失败分支补 WARN 日志） |

无 P0/P1 缺陷。D1 为发布前置条件（§7 附条件①）。

## 5. 体检条目 → 处理状态对照表

| 体检条目 | 级别 | 处理状态 | 验收依据（§） |
| --- | --- | --- | --- |
| P0-1 详情页未接真实接口 | P0 | **已修复，验证通过** | §3.2（mock 删/by-code/三态/负缓存） |
| P0-2 refresh 令牌可当 access 用 | P0 | **已修复，验证通过** | §3.2（四场景 401/1003） |
| P0-3 政策倾向无限重试烧预算 | P0 | **已修复，验证通过**（附 R3 观察项） | §3.2（四侧证据+EXHAUSTED） |
| P1-1（任务书④）SSE 零消费+push 积压 | P1 | **已修复，验证通过** | §3.3（全链路+心跳+有界扫描） |
| P1-2（⑤）标的只能数字主键添加 | P1 | **已修复，验证通过** | §3.3（搜索多场景+quotes+选择器） |
| P1-3（⑥）订阅无管理入口+CTA 指错 | P1 | **已修复，验证通过** | §3.3（四类型 CRUD+CTA 指向） |
| P1-4（⑦）首页视角错位+推荐无消费 | P1 | **已修复，验证通过** | §3.3（两段式+推荐卡数据） |
| P1-5（⑧）feed 同步阻塞无总预算 | P1 | **已修复，验证通过** | §3.3（8ms+pending 字段） |
| P1-6（⑨）简报 FAILED 占坑幂等键 | P1 | **已修复，验证通过** | §3.3（重试受理+预算后置） |
| P1-7（⑩内）Policy/JobLog 加载更多清空 | P1 | **已修复，验证通过** | §3.3（loadMoreError） |
| P1-8（⑩内）Watchlist 移除无确认 | P1 | **已修复，验证通过** | §3.3（确认 Dialog） |
| P2 徽章语义色两套并存 | P2 | **已修复（择要），验证通过** | §3.3（两 Badge 语义色+测试） |
| P2 SubjectDetail/Watchlist 错误无重试 | P2 | **已修复（择要），验证通过** | §3.3（重试按钮+用例） |
| P2 TaskCenter 编辑丢输入/RESTART 徽章 | P2 | **已修复（择要），验证通过** | §3.3 |
| P2 Policy/JobLog 下拉随过滤收缩 | P2 | **已修复（择要），验证通过** | §3.3（全量缓存） |
| P2 FAILED/MISSING 不负缓存 | P2 | **已修复（择要），验证通过** | §3.2/§3.3（missing 分区毫秒返回） |
| P2 ai_brief 并发撞唯一索引 500 | P2 | **已修复（择要），验证通过** | §3.3（败者重查同任务） |
| P2 SSE 无心跳帧 | P2 | **已修复（择要），验证通过** | §3.3（实测 :heartbeat） |
| P2 SubjectDetail AI 简报不带参 | P2 | **已修复（择要），验证通过** | 源码 `#/ai-brief?subjectId=` 预填+回显 |
| P2 Feed 阅读埋点未接 | P2 | **部分**：Policy/SubjectDetail 已挂 trackReadingOnce，**Feed 页未挂** | 源码核对（遗留 backlog） |
| P2 数据源失败事件不节流（写放大） | P2 | **未修**——QUOTE errors24h 6491（体检时 1950，恶化 3.3 倍） | R1 遗留风险 |
| P2 异动去重无方向粒度 / 幻觉校验覆盖窄 / 1 万行护栏静默截断 / AiBrief 轮询无超时 / Dialog 焦点圈定等 | P2/P3 | **未列入本批**（backlog） | — |
| P3 oxlint load-in-effect 存量族 | P3 | **未修**（25 warnings 0 error，独立技术债任务） | §3.1 |

## 6. 测后数据恢复（红线核销）

| 步骤 | 结果 |
| --- | --- |
| 测前备份 | `VACUUM INTO '/tmp/m6-regression-backup-20260922.db'`（2.6 MB，`PRAGMA integrity_check`=ok；未裸拷贝，规避 WAL 不一致） |
| 测试写入清单 | anomaly_event×1（注入）、push_record×1（链路产物）、subscription_config×3（四类型用例）、ai_brief×3（伪造 FAILED/DONE+重试产物）、llm_call_log×2（P1-9 异步真实调用） |
| 测后清理 | 精准 DELETE 上述 9 行（保留 job_execution_log 系统自然轮转日志 78 行——Job 每 10s/30s 运转留痕，非测试数据状态，删除反破坏运行连续性） |
| 恢复核验 | 7 表行数与备份逐一 **MATCH**（anomaly_event 0/push_record 0/subscription_config 1/ai_brief 5/llm_call_log 125/policy_item 13/reading_event 14）+ 残留痕迹 0 + integrity ok |
| 基线延续 | 服务全程未重启，`/tmp/m6-regression-backup-20260922.db` 保留备查（重启后自动清除临时目录前可复用） |

## 7. Go/No-Go 与遗留风险

**结论：Go（附条件）**

- 依据：P0×3、P1×8 体检主项全部端到端验收通过；后端 1085 + 前端 229 自动化全绿；无未清 P0/P1 缺陷（红线满足）。
- **附条件①（发布前置）**：D1 spotless 违规修复（`mvn spotless:apply` + 完整 `mvn verify` 重跑通过，<0.5h，纯格式不改行为）——若团队以完整 verify 为发布硬门禁，则此条件未完成前不得发布。
- 附条件②（建议，不阻塞）：上线后第 13+2 页（新增订阅页 + 通知铃铛）人工浏览器点检一轮（≤10 分钟）。

遗留风险与跟进建议：

| # | 风险 | 建议 |
| --- | --- | --- |
| R1 | QUOTE 源 errors24h=6491 且持续增长（体检 1950 → 3.3 倍，data_source_event 写放大未节流） | P2「失败事件同源节流/最小熔断」建议提前排期（ADR-0010 接口位已留）；否则单写者 SQLite 表增长与 IO 压力持续恶化 |
| R2 | 本轮无浏览器自动化，前端交互以组件测试+构建产物+源码为据 | 上线前人工点检一轮（附条件②） |
| R3 | P0-3「attempts=3 停扫」终态无线上实例（退避窗内） | 次日 10:00 后点检 `SELECT tendency_attempts, COUNT(*) FROM policy_item WHERE ai_tendency=0 GROUP BY 1`——预期 attempts 封顶 3 后 UNJUDGED 存量不再产生新 scene3 调用 |
| R4 | D2 口径歧义（llmToday 分子含系统调用） | 48h SLA 歧义单回 PM/架构：分子切 per-user 或标签改「平台总消耗」 |
| R5 | D3 挂起（SUCCESS 留痕 vs FAILED 终态不一致） | 开发补解析失败分支 WARN 日志后复现定位；不影响主流程（重试入口可用） |
| R6 | Feed 阅读埋点仍未接（推荐画像数据源缺口，设计 §3.5 交互 4） | backlog 排期（Policy/SubjectDetail 已挂可参考实现） |

## 8. 覆盖率与用例统计

- 接口级用例执行：**43 条**（P0 组 11 + P1 组 26 + 安全/对照 6），42 通过 + 0 失败（2 条勘误为调用方参数错误重测通过，非缺陷）。
- 自动化：后端 1085（本轮零新增——集成验收定位，回归靠各批自测用例 + 本报告 E2E 证据）；前端 229。
- 本轮验证方式占比（如实披露）：接口级 E2E 为主（真实运行服务），浏览器 GUI 0 条（会话无浏览器工具），前端行为以组件测试/源码/构建产物三重证据替代并逐处标注。

> 报告完。测试工程师对上述 Go 结论负责：P0/P1 清零红线满足，D1 为唯一发布前置项。
