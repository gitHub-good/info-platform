# 🧪 测试报告 · 信息整合与 AI 分析平台 M4（T34~T44 配置与运维可视化）

> 测试编号：TEST-20260922-M4 ｜ 测试人：测试工程师（虚拟团队）｜ 日期：2026-09-22
> 提测范围：T34 配置中心底座 + T35/T39 LLM 模型配置 + T36/T40 数据源配置 + T37/T41 任务执行中心 + T38/T44 统一导航 + T42 概览仪表盘 + T43 个人信息流（11 任务，前端页面 12 个）
> 代码基线：main @ `66a78d3`（工作区干净，与提测提交清单一致；开发自测口径 890/135 与本报告复跑结果一致）

## 1. 结论先行

| 维度 | 结果 |
| --- | --- |
| 自动化回归 | **全绿**：后端 `mvn verify` 890/890（0 失败 0 错误 0 跳过，jacoco 整体行覆盖 93.8%）；前端 vitest 135/135、oxlint 16 warnings 0 error（存量族，与提测口径一致）、`npm run build` 通过 |
| 接口冒烟 | **57/59 通过**：两种运行模式（CONFIG_SECRET / 无）各一轮真实服务冒烟，2 条发现缺陷（见 §4） |
| 验收场景 | PRD 27 场景 + PM 五红线全部覆盖：**26 场景通过**（其中 2 个附说明），**1 场景（3.2 切 mock 即生效）因 P1 缓存缺陷在缓存预热态不通过** |
| 缺陷 | **P1 × 1（缓存条目永不过期）、P3 × 2**；另有观察项 4 条（不开单） |
| Go/No-Go | **No-Go——P1 未清零（质量一票否决）**，修复面小，修复 + 回归后可复审放行，依据见 §6 |

一句话：M4 的配置中心、热生效机制、任务调度中心、脱敏与加密、重启持久化、降级链路**架构语义全部实证达成**（含五条 PM 红线全过），但在数据源/AI 缓存的 TTL 过期语义上发现一个 P1 缓存缺陷——被读过的缓存条目永不过期，会把「切 mock 即生效」遮蔽成「重启前一直旧数据」，并连带冻结真实模式的行情新鲜度。

## 2. 范围与环境

### 2.1 测试范围（与提测单一致）

| 任务 | 交付要点 | 主要依据 |
| --- | --- | --- |
| T34 | runtime_config 权威存储（V15/U15）、ConfigCenter 不可变快照 + 写时替换 + 变更事件、yml 种子 seed-if-absent、三域表驱动校验器、错误码 30060~30065、AES-256-GCM 密文工具 | 技术方案 §4.1/§4.2、ADR-0017/0018 |
| T35/T39 | llm-config 组接口（全局/PATCH、provider/PUT、key 只写、连通性测试）+ 消费点热改（网关/守卫/缓存/provider）+ #/llm-config 页 | PRD 故事 2、§4.4.1 |
| T36/T40 | 7 源 GET/PATCH + 聚合总超时 + RoutingSourceAdapter 热路由 + OK 心跳健康聚合 + #/datasource-config 页 | PRD 故事 3、§4.4.2 |
| T37/T41 | ManagedJob/JobRegistry/JobScheduler/JobExecutor（防重入）+ 5 Job 收编 + jobs 组接口 + #/task-center 页 | PRD 故事 4、§4.5 |
| T38/T44 | AppLayout 统一导航（4 分组 12 项）、窄屏抽屉、默认落地 /overview、既有页收编修补 | PRD 故事 1、UI 方案 §2 |
| T42 | GET /api/v1/overview 五卡片聚合 + #/overview 页 | PRD 故事 5、§4.6 |
| T43 | #/feed 条目流 + 关键词高亮 + 哨兵游标分页 + 双空态（阅读埋点延后按 ADR-0019） | PRD 故事 6、ADR-0019 |

不在本次范围：M1~M3 已交付业务功能的行为（由 890 条自动化回归承接）；信息流阅读埋点（ADR-0019 明确延后）。

### 2.2 环境与方法

- 后端：Java 21 + Spring Boot 3 + SQLite（WAL）+ Flyway V15，`mvn spring-boot:run` 真实启动 **两轮**：
  - **模式 1**：`JWT_SECRET` + `CONFIG_SECRET`（key 加密写入可用）；
  - **模式 2**：仅 `JWT_SECRET`（30064 降级只读态），并承接「重启后配置恢复」验证。
- **前端验证方式（如实标注）**：本会话无浏览器自动化工具，前端交互以 **组件/页面测试（vitest 135 条，含 M4 六页 59 条）+ 生产构建产物** 为依据；导航、脱敏回显、防重入置灰、XSS 高亮等红线断言已在测试代码中逐条核对（§3.3）。未做真实浏览器点检。
- 沙箱网络（部分外网可达，恰好覆盖关键场景）：gov.cn **可达**（POLICY_FETCH 真实抓取成功）、api.deepseek.com **可达**（伪造 key 获真实 401，验证连通性测试的错误传播）、push2.eastmoney.com **不可达**（行情 REAL 模式表现为超时→MISSING，恰好用于验证降级语义）。
- 无真实 LLM key：SUCCESS 成本路径（token 计数/微元估算/预算真实消耗）未做端到端实证，以单测为依据（提测单已知口径，遗留风险 R1）。
- 数据治理：测试前 `VACUUM INTO` 备份 + 逐表记录基线行数（user 1 / runtime_config 18 / job_execution_log 81 / policy_item 5 / llm_call_log 1 / data_source_event 21 / ai_brief 2）；测试增量（手动触发的 job_execution_log、连通性测试 llm_call_log、OK/超时事件、ai_brief 1 行、runtime_config 变更）已全部通过 `sqlite3 .backup` 恢复至基线清除，恢复后逐表核对一致（遵循 M3 事故教训：**备份恢复一律 sqlite3 backup API，未裸拷贝**）。

## 3. 用例矩阵与结果

用例来源 = REQ-20260922-02 的 27 个 Given/When/Then + PM 五条验收红线 + 技术方案契约 + 04 测试范围矩阵（边界/异常/并发/安全）。优先级：P1 核心链路 / P2 重要分支 / P3 次要。

### 3.1 自动化回归（金字塔底座）

| 套件 | 结果 | 说明 |
| --- | --- | --- |
| 后端 `mvn -q verify`（119 测试类） | 890/890 通过 | 含 T34~T44 新增：RuntimeConfig* 套件、ConfigCenterTest、ConfigSecretCipherTest、LlmConfig*/DataSourceConfig*/Job* 控制器与校验器套件、JobScheduler/JobExecutor/JobRegistry、RoutingSourceAdapterTest、OverviewServiceTest 等 |
| jacoco 覆盖率 | 整体行覆盖 35486/37833 = **93.8%** | M4 新增/核心类逐类核对：RuntimeConfigService 95%、ConfigCenter 80%、ConfigFieldRules 95%、ConfigSecretCipher 88%、JobScheduler 92%、JobExecutor 100%、RoutingSourceAdapter 100%、OverviewService 99%、LlmConfigController 88% 等，区间 80%~100%，**增量门禁 ≥80% 达标**（ConfigCenter/AggregationRuntimeConfigSeeder 恰在 80% 临界，建议修复 P1 时顺带补时序分支用例） |
| 前端 `npx vitest run` | 135/135 通过（19 文件） | M4 六页齐备：LlmConfig 12、DatasourceConfig 9、TaskCenter 11、Overview 8、Feed 10 + KeywordHighlight 9、AppLayout 7、navigation 4 |
| 前端 oxlint / build | 16 warnings 0 error（存量族口径一致）/ 构建通过（392 KB，gzip 118 KB） | — |

### 3.2 接口冒烟（真实服务，59 条）

**鉴权与安全（A 组，3 条全过）**

| # | 用例（优先级/依据） | 结果 |
| --- | --- | --- |
| A1 | 无 token 访问 llm-config → 401/1003（P1） | ✅ `code=1003 未提供认证令牌` |
| A2 | 错误凭证登录 → 401/1001（P1） | ✅ `code=1001 凭证错误` |
| A3 | admin 登录 → 200 + accessToken（P1） | ✅ |

**T35 LLM 配置域（B 组，24 条：22 过 + 2 缺陷关联见 §4）**

| # | 用例（优先级/依据） | 结果 |
| --- | --- | --- |
| B1 | GET 全量视图：4 provider（名称/模型/启用/默认/fallback/单价/超时）+ 全局参数（预算/阈值/TTL 分档/重试/缓存上限）均来自当前生效配置；`effectiveModes` 逐字段标 LIVE/RESTART（P1/场景 2.1） | ✅ cacheMaximumSize=1000 只读随 GET 返回；baseUrl 标 RESTART |
| B2 | 改预算 20000→5000 保存后**不重启**：成本报表 `dailyBudgetTokens` 即时同步 5000、概览卡 `budgetTokens` 同步 5000（P1/场景 2.2、红线 1） | ✅ 热生效 + 两个消费点同步实证 |
| B3 | 改单价 1.0→2.0：GET 即新值、`effectiveModes` LIVE；`llm_call_log` 存量行 cost_micros 不重算（P1/场景 2.3、红线 5） | ✅ 历史留痕不动；新调用按新价的 SUCCESS 路径留单测依据（无真实 key，R1） |
| B4 | isDefault 互斥：置 glm 默认 → deepseek 自动置反，全局恰一（P1/契约） | ✅ defaults_count=1 |
| B5 | key 写入（CONFIG_SECRET 模式）：写响应**不含明文**、GET 仅 `status=CONFIGURED/source=DB/last4`（尾 4 位正确）、DB 存 AES 密文（base64，无明文字符串）、服务日志 grep 明文 **0 次命中**（P1/场景 2.4、红线 2） | ✅ 响应/GET/DB/日志四层验证全过 |
| B6 | 连通性测试：deepseek（DB 伪造 key）→ 真实 401 传播 `{ok:false,error:"401 Authorization Required"}`；glm（无 key）→ 401；scene_key=test 落 llm_call_log（P2/§4.4.1） | ✅ 126ms/99ms 真实外呼，密文 key 经 DB>ENV 解析被实际使用 |
| B7 | 非法值拦截 12 项：预算 0/负、阈值 1.5/0、默认 TTL 0/分档负、retry 9、timeout 0、单价负、model 空、fallback 指向不存在、baseUrl 非法 → 全部 400/2001 字段级原因，原值不变（P1/场景 2.5、非功能校验） | ✅ 12/12 |
| B8 | expectedUpdatedAt 过期值 → 409/30065 并发防呆（P2/§4.1） | ✅ |
| B9 | bogus provider → 404/30060（P3） | ✅ |
| B10 | 降级态（无 CONFIG_SECRET）：key 写入 → 503/30064「key 暂走环境变量」；GET 视图不报错（P1/ADR-0018、场景验收降级态） | ✅ 模式 2 实证 |
| B11 | 重启不丢：模式 1 改预算 5000 → 停服 → 模式 2 重启 → 仍 5000（yml=20000 未覆盖），单价等其余键同样恢复（P1/场景 2.6、红线 4） | ✅ seed-if-absent 语义实证 |
| B12 | 重启后 DB 密文在无 CONFIG_SECRET 下不可解 → 连通性测试回落 ENV（空）→ 401 而非 500（P2/ADR-0018 轮换语义） | ✅ 降级不崩 |

**T36 数据源配置域（C 组，15 条：13 过 + 1 缺陷 D1 + 1 观察项）**

| # | 用例（优先级/依据） | 结果 |
| --- | --- | --- |
| C1 | GET 7 源卡：启用/模式/超时/重试 + health（lastEventType/lastEventAt/errors24h）+ 聚合总超时块（P1/场景 3.1） | ✅ QUOTE=OK 心跳 + errors24h=4（与 SQL 24h 窗口逐条对上）；ANNOUNCE 从无事件 → lastEventType=null（场景 3.5 空态） |
| C2 | 切 mock 即生效：MOCK→REAL 保存后详情聚合走 REAL 分区（P1/场景 3.2） | ⚠️ **部分不通过（DEFECT-1）**：重启后新鲜缓存下 REAL 立即生效（detail quote=MISSING，eastmoney 超时 1.5s→不缓存）、切回 MOCK 立即 mock 数据 ✅；但**缓存已有 OK 条目并曾被读取后**（生产稳态），REAL 切换被永不过期的旧缓存遮蔽，直到重启 |
| C3 | 聚合总超时 2000→3000：`PATCH /aggregation/global` 即时生效（LIVE 每请求读取）+ 边界 100/99999 拒绝（P2/场景 3.3） | ✅ 值变更与 500~10000 边界拦截实证；请求时延观测受沙箱网络限制，以契约与单测为补充依据 |
| C4 | 非法值 7 项：timeout 0/负、retries 9、TTL 0、mode 枚举非法、params.quoteUrl 非法、未知源 30061（P1/场景 3.4） | ✅ 7/7 字段级原因 |
| C5 | enabled=false 短路：detail 该分区立即 MISSING 不外调（P2/§4.2 路由语义） | ✅ 路由层读到最新配置（与 C2 对照锁定缺陷在缓存层） |
| C6 | 连通性测试：REAL 超时传播 `{ok:false, latencyMillis:1518, mode:REAL}`；EVENT 源本地表语义 `{ok:false, note:"事件源读取本地异动表，无外部端点"}`（P2） | ✅ 已知「EVENT 无数据 ok=false 偏严格」与提测交底一致 |
| C7 | EVENT 源 PATCH mode/timeout 被后端接受（P3/观察项 O1） | ⚠️ 后端契约未排除 EVENT（UI 层已按设计只读化，前端测试断言无 mode 控件）；见 §5 观察项 |

**T37 任务执行中心（D 组，11 条全过）**

| # | 用例（优先级/依据） | 结果 |
| --- | --- | --- |
| D1 | GET /jobs：5 任务全字段（开关/间隔或 cron/上次执行时间状态耗时/下次执行/running）（P1/场景 4.1） | ✅ |
| D2 | 手动触发 POLICY_FETCH → 202 + `{executionId:128, status:STARTED}`；job_execution_log STARTED→SUCCESS 留痕（103ms，start/end 齐全）（P1/场景 4.2） | ✅ |
| D3 | 防重入：运行中立即再触发 → 409/30063「任务正在运行」（P1/场景 4.4、红线 3） | ✅ |
| D4 | 触发见效：手动抓取成功且 gov.cn 真实可达（P2/场景 4.3） | ✅ 附说明：本轮 gov.cn 列表未轮换，去重后无新增条目（去重语义正确），政策页 5 条数据可读，触发→落库→页面链路由执行 SUCCESS + 数据在场证明 |
| D5 | 从未执行空态：DAILY_RECOMMEND `lastExecution=null` + enabled=false 时 `nextExecutionTime=null`（P2/场景 4.5） | ✅ |
| D6 | 启停热切换：停 ANOMALY_DETECT/PUSH_RETRY 后 25s 无任何新执行记录（原 10s/30s 高频调度）；PATCH 响应 `effectiveModes={enabled:LIVE, interval/cron/userIds:LIVE_NEXT_CYCLE}` 明示（P1/场景 4.6） | ✅ 立即停 + 生效方式契约明示 |
| D7 | 间隔热切换：POLICY_TENDENCY 1800000→600000 保存后 nextExecutionTime 按「上次完成+新间隔」重算（22:05:25+600s=22:15:25），到点**真实触发一次执行**（executionId=129）；改回 1800000 正常重注册（P1/场景 4.6） | ✅ 补跑行为与「下一周期生效」语义一致 |
| D8 | 手动触发**已停用**任务 DAILY_RECOMMEND → 202 受理并执行成功（P2/ADR-0017 调度与执行解耦） | ✅ 旧「bean 未装配无法触发」问题消除 |
| D9 | 每日推荐双通道幂等联测：手动触发后立即 GET /recommendations/daily → 复用当日 brief（topN 一致、两次 GET 结果一致，无重复生成）（P2/提测交底建议项） | ✅ |
| D10 | 未知 jobKey → 404/30062；非法 cron → 2001（附 6 段格式说明）；interval 0 → 2001（P2/错误码） | ✅ |
| D11 | 任务详情 GET /jobs/{jobKey}：job 全字段 + 最近执行（P3） | ✅ |

**T42 概览仪表盘（E 组，2 条全过）**

| # | 用例（优先级/依据） | 结果 |
| --- | --- | --- |
| E1 | 五卡片聚合：llmToday（今日日界 windowStart=北京时间 00:00、budgetTokens=运行时当前值 5000 随改随新、FAILED 0 token 不计）、anomalyToday=0、policy24h count=5 + latest 5 条、jobHealth windowRuns=130 与 SQL 24h 窗口 COUNT 交叉一致、sourceHealth 逐源与 data_source_event 对上（P1/场景 5.1、§4.6 口径） | ✅ 三类口径（今日日界/滚动 24h/运行时预算）交叉核对一致；探活剔除见观察项 O3 |
| E2 | 空态：anomalyToday=0 正常返回不报错；全零空数组路径由 OverviewServiceTest（行覆盖 99%）承接（P2/场景 5.2） | ✅ |

**T43 个人信息流（F 组，4 条：3 过 + 1 缺陷 D2）**

| # | 用例（优先级/依据） | 结果 |
| --- | --- | --- |
| F1 | GET /feed/personal：命中流按时间倒序（recommendation/policy 混排）、`keywords[]` 命中关键词透出（贵州茅台）、matchReason 可判定（P1/场景 6.1） | ✅ |
| F2 | 游标分页：cursor（上一页末条 id 数值）翻页正确、末页 nextCursor=null 哨兵（P1/场景 6.1） | ✅ |
| F3 | 非法 cursor（`zzz`）→ 应优雅拒绝（P3） | ❌ **DEFECT-2**：返回 500/50000 而非 400/2001 |
| F4 | 空态（无订阅引导/无条目）：运行时不可构造（单用户系统且无注册接口，admin 已有订阅）→ 以 Feed.test.tsx 双空态用例为依据（P2/场景 6.2） | ✅（组件级，如实标注） |

**跨模式与回归（G 组，4 条全过，与 B10~B12 有交叉不重复计数）**

| # | 用例 | 结果 |
| --- | --- | --- |
| G1 | 模式切换（CONFIG_SECRET→无）重启后：runtime_config 全部 18 键恢复、种子不覆盖页面值 | ✅ |
| G2 | 密码错误/限流、JWT 1h 有效性跨重启（同 JWT_SECRET）不受影响 | ✅ |
| G3 | 新鲜缓存对照实验：重启后 REAL→MISSING（不缓存）→切 MOCK→立即 mock 数据（因果链闭合，锁定 DEFECT-1 根因在缓存层而非路由/配置中心） | ✅ |
| G4 | 全部配置改回基线后重启一轮：GET 三域视图与种子一致（无脏数据残留） | ✅（最终以 DB 基线恢复收尾） |

### 3.3 前端组件测试红线断言核对（vitest 135 内抽取）

| 红线 | 断言位置 | 核对结果 |
| --- | --- | --- |
| key 明文永不回显 | `LlmConfig.test.tsx`：录入 `sk-live-9876` 保存成功后 `queryByText(/sk-live-9876/)` 为 null；缓存上限只读 + RESTART 徽章 | ✅ |
| 防重入按钮态 | `TaskCenter.test.tsx`：运行中行「立即执行」置灰为「运行中…」（PRD 场景 4.4）；409/30063 收敛路径 mock | ✅ |
| 导航全量可达 | `AppLayout.test.tsx`：4 分组 12 项导航、当前项 aria-current 高亮、窄屏抽屉 | ✅ |
| XSS 安全高亮 | `KeywordHighlight.test.tsx`：`<script>` 注入文本+关键词均以纯文本渲染，页面无 script 元素 | ✅ |
| 30064 只读降级 | `LlmConfig.test.tsx`：key 区只读态（走环境变量提示） | ✅ |
| 下一周期生效明示 | `TaskCenter.test.tsx`：间隔/cron 调整提示下一周期生效 | ✅ |

## 4. 缺陷清单（定级与复现）

### DEFECT-1【P1】SourceCache / LlmCache 条目被读取后永不过期——Caffeine Expiry 语义误用

- **现象**：数据源 mode 热切换在「缓存已有 OK 条目且曾被读取」的稳态下不生效；缓存 TTL（含页面热改）对存量条目无效。
- **根因**：`SourceCache.java`（SourceExpiry）与 `LlmCache.java`（LlmExpiry）的 `expireAfterRead` 均返回 `Long.MIN_VALUE`，注释假设「= 不变」。**Caffeine 3.1.8 实测语义并非不变，而是使条目永不过期**（离线探针：put + 5s TTL + 读一次 → 40s 后仍命中；对照组 `expireAfterRead` 返回 `currentDuration` 则 5s 正常过期）。
- **复现步骤**（模式 1，沙箱 mock）：
  1. `GET /api/v1/subjects/1/detail?sections=quote` 两次（第二次命中缓存，即完成「读」）；
  2. `PATCH /api/v1/datasource-configs/QUOTE {"mode":"REAL"}`（响应/GET/DB 三处确认 mode=REAL）；
  3. 等待 >TTL（12~15s）后再次 detail → **仍返回 mock 旧数据（1680.5/123456/9876543210）**，sourceStatus=ok；
  4. 旁证：期间无新 OK 心跳、无 ERROR/TIMEOUT 事件（未发生真实取数）；重启后同操作立即正确（REAL→MISSING、MOCK→mock）。
- **影响**：① PRD 场景 3.2「切 mock 即生效」在稳态不生效（需重启），Should 级验收场景不通过；② 真实模式行情 5s TTL 失效 → 行情冻结 → **异动检测/推送链路（M1 核心故事）静默退化**；③ LlmCache 同族：AI 简报 1h / 每日推荐 24h TTL 失效，简报永不过期；④ 缓存 TTL 热改对存量条目无意义。
- **为什么自动化没拦住**：`SourceCacheTest`/`LlmCacheTest` 仅覆盖 put/get/分区/供应器语义，**无任何基于时间的过期用例**（未注入 Caffeine Ticker）。
- **修复要求**：`expireAfterRead` 返回 `currentDuration`（对照组已验证）或等价语义；**必须附带能复现本缺陷的失败用例**（Ticker 假时钟：写→读→推进 TTL→断言 miss），SourceCache 与 LlmCache 各至少一条；修后全量回归 + 本报告 C2/G3 用例复测。
- **定级依据**：核心功能异常（热生效红线场景 + M1 异动链路退化 + 简报新鲜度），有绕行方案（重启可短期恢复）→ P1，24h 内修复。

### DEFECT-2【P3】Feed 游标分页非法 cursor 返回 500

- **复现**：`GET /api/v1/feed/personal?cursor=zzz` → HTTP 500 `{code:50000,msg:服务异常}`。
- **期望**：参数类型不匹配应 400/2001（与三域配置校验口径一致）。前端哨兵游标逻辑不会发出坏 cursor，实际触发面低。
- **定位**：`FeedController.personal` 的 `@RequestParam Long cursor` 类型转换异常被全局兜底捕获为 50000。建议在 GlobalExceptionHandler 增加类型不匹配映射。本迭代内修复。

### DEFECT-3【P3】provider fallback 校验不完整——可指向已禁用 provider，静默失去 fallback 保护

- **复现 a**：`PUT /api/v1/llm-config/providers/deepseek {"fallback":"kimi"}`（kimi enabled=false）→ 200 接受；技术方案 §4.1 键空间表要求「fallback 须为存在且 **enabled** 的 provider」。
- **复现 b**：`PUT .../providers/deepseek {"enabled":false}`（glm/qwen/kimi 均引用 deepseek 作 fallback）→ 200 接受，无交叉校验或告警。
- **影响**：运行时 fallback 链跳过禁用 provider（`LlmGatewayImpl` 已按 enabled 过滤，无报错），用户以为有灾备实际没有。建议：写入时校验拒绝，或禁用被引用 provider 时返回告警信息。本迭代内修复。

### 缺陷统计

| 级别 | 数量 | 状态 |
| --- | --- | --- |
| P0 | 0 | — |
| P1 | 1（DEFECT-1） | 新建，待开发 24h 内修复 |
| P2 | 0 | — |
| P3 | 2（DEFECT-2/3） | 新建，本迭代内修复 |

## 5. 观察项与遗留风险

**观察记录（不开单，供开发/架构知悉）：**

1. [O1] EVENT 源后端 PATCH 接受 mode/timeout 修改（后端契约按 7 源统一，未排除 EVENT；UI 已按设计只读化且前端测试断言无该控件）。建议后端补防御性拒绝（防呆一致性），非缺陷。
2. [O2] 概览 policy latest 在 created_at 同秒并列（种子数据为日期粒度）时按 id 兜底排序，展示顺序与 publishedAt 逆序不一致——种子数据伪像，真实抓取时间戳到秒不受影响。
3. [O3] 概览 llmToday「探活剔除」：本会话全部 llm_call_log 均为 FAILED（0 token），运行时无法与「FAILED 不计」区分实证，以 OverviewServiceTest（覆盖 99%）为依据。
4. [O4] `GET /recommendations/daily` 在自选池标的仅 1 个时输出 Top1（候选池决定上限）——M3 已知行为，非缺陷。

**遗留风险与跟进：**

| # | 风险/事项 | 等级 | 说明与建议 |
| --- | --- | --- | --- |
| R1 | 无真实 LLM key：SUCCESS 成本路径（token 计数/微元估算/预算 REJECTED 拦截/缓存命中）未端到端实证 | 中 | 单测覆盖（网关五态留痕断言）；上线首日用真实 key 触发简报 + 每日推荐，核对 llm_call_log SUCCESS 行与报表 costMicros>0（沿用 M3 R1 跟进口径） |
| R2 | DEFECT-1 修复后的复测窗口 | 高 | 修复必须带 Ticker 过期回归用例；修复后除 890 全量外，需复测本报告 C2（缓存毒化态切模式）与 G3（新鲜缓存对照）两条 E2E |
| R3 | llm_call_log / reading_event 清理任务（M4 候选未做）、每日推荐幂等键全局单份 | 低 | ADR-0015/0016 已知取舍，M5+ 排期（与提测交底一致） |
| R4 | 前端未做真实浏览器点检（本会话无浏览器工具） | 低 | 组件测试 + 构建产物为依据；建议上线前人工过一遍 12 页导航与窄屏抽屉（≤10 分钟） |
| R5 | oxlint 16 warnings 存量族 | 低 | 与提测口径一致（0 error 门禁），随迭代消化 |

## 6. Go/No-Go

### 结论：**No-Go（P1 未清零，质量一票否决）**

依据（对齐 04 测试规范 DoD）：

- ✅ 验收标准覆盖：27 场景 + 五红线均有用例承接（自动化 890+135 + 冒烟 59），矩阵主路径/边界/异常/安全逐项过
- ✅ 覆盖率：整体 93.8%（≥60%），M4 新增类 80%~100%（≥80% 增量门禁），抽验有效（口径交叉核对一致，未见恒真断言凑数）
- ❌ **缺陷：P1 × 1 未清零（DEFECT-1 缓存条目永不过期）→ 按红线不得放行，无人可 override**
- ✅ 回归与冒烟：自动化全绿；冒烟 57/59，2 条失败均已开单定位到根因

**复审路径（修复面小，预期一轮短环）**：

1. 开发修复 DEFECT-1（两处 `expireAfterRead` 返回值 + SourceCache/LlmCache 各补 Ticker 过期回归用例，修前红修后绿）；
2. 全量回归 890 + 复测 C2/G3 两条 E2E（含缓存毒化态切 mock/REAL、行情 TTL 到期刷新）；
3. 顺带处理 DEFECT-2/3（P3，可与 P1 同批）；
4. 复审通过即转 Go，其余维度（覆盖率/红线/回归）本轮已实证无需重测。

**Go 后上线附带条件**：R1 真实 key 首日回归（沿用 M3 口径）；R4 上线前 12 页人工点检一轮。

---

方法论与角色依据：个人知识库 [04 测试规范](/project-development/04-testing/index.md)；验收标准来源：[REQ-20260922-02](../01-需求/REQ-20260922-02-配置与运维可视化.md)；契约依据：[技术方案-配置与运维可视化](../02-设计/技术方案-配置与运维可视化.md)、[UI 方案 §7](../02-设计/ui-配置与运维可视化.md)、[ADR-0017/0018/0019](../02-设计/adr/README.md)。
