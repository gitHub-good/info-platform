# 🧪 测试报告 · 信息整合与 AI 分析平台 M9（列表分页与查询条件 T60~T65）

> 测试编号：TEST-20260925-M9｜ 测试人：测试工程师（虚拟团队）｜ 日期：2026-09-25
> 提测范围：REQ-20260925-06（4 故事 16 场景）全量交付 T60~T65——后端 `5bd0e71`（T60）/`090cdc1`（T61）/`dfb60bf`（T62）+ 前端 `cd8bbf3`（T63）/`fbfd415`（T64）/`0bc45ba`（T65）
> 验收依据：[REQ-20260925-06-列表分页与查询条件.md](../01-需求/REQ-20260925-06-列表分页与查询条件.md)（16 场景逐条转化）+ [技术方案-列表分页与查询条件.md](../02-设计/技术方案-列表分页与查询条件.md) §4 契约/§6 测试要点（ADR-0035）+ [ui-列表分页与查询条件.md](../02-设计/ui-列表分页与查询条件.md) §5 联动语义/§7 交接要点
> 代码基线：main @ `0bc45ba`（工作区仅 untracked `backend/.gitignore`，M7 起的本地遗留，与本轮无关）
> 服务基线：8080 后端 HEAD 进程（分页接口已冒烟：政策关键词 total=2@提测时、Job FAILED total=7）+ 5173 Vite（HMR 已热更）；admin 实账号 JWT

## 1. 结论先行

| 维度 | 结果 |
| --- | --- |
| 自动化全量（后端） | **1356/1356**（0 失败 0 错误 0 跳过，`mvn verify` BUILD SUCCESS 含 spotless/jacoco 门禁，与开发自测基线一致，只增不减）；其中 M9 触及 8 个测试类 **109 条**全绿（PageQuery 8 / PolicyController 17 / JobLogController 14 / PolicyService 13 / JobLogQueryService 4 / PolicyRepositoryImpl 32 / LikePattern 6 / JobExecutionLogRepositoryImpl 15） |
| 自动化全量（前端） | **278/278**（26 文件，vitest run）+ oxlint **0 errors**（27 warnings 为存量收敛项，README backlog 在册）+ `vite build` 成功（465.52 kB / gzip 136.65 kB） |
| 接口级 E2E（8080 真实服务） | **65/65 全过**：双模式共存锚点 5、参数校验矩阵 27、组合过滤计数 21（SQLite 只读真值交叉验证）、深分页与性能 4、安全鉴权 6 + 游标续翻链路 3 跳运行时验证 |
| 前端交互（无浏览器工具，三重证据） | 组件/页面测试断言质量抽验**非恒真** + 构建产物 testid 全量在场且「加载更多」链路零残留 + 源码逐条比对 UI 设计 §5 联动语义——**如实标注：非浏览器实走**（§3.3） |
| 回归红线 | Feed 无限滚动/订阅页游标/详情列/401 跳登录/阅读埋点——**diff 零改动**（13 红线文件 clean）+ 游标分支原样保留 + 运行时续翻验证；游标模式响应形状不变（`policies/nextCursor`，无 `total/page/size` 混入） |
| 缺陷 | **P3 × 2（均为文档级勘误，非产品缺陷）**；无 P0/P1/P2 |
| Go/No-Go | **Go**——P0/P1 = 0；16 验收场景全部有运行时或自动化证据承载；数据零破坏（§6） |

一句话：T60~T65 的契约确定性（互斥 400/越界空页/如实回显/拒绝式 size 上限）在真实服务上逐条实证，组合过滤计数与 SQLite 真值全对齐，双模式「page 缺席游标不变」由 diff + 形状 + 运行时三面证据闭合；唯一方法限制是前端无浏览器实走，由三重证据承载（§3.3/§7 R1）。

## 2. 范围与环境

### 2.1 验收场景 → 证据来源（PRD 16 场景全对照）

| PRD 场景 | 验收要点 | 证据承载 |
| --- | --- | --- |
| 1.1 首页与总数 | 最新 20 条 + 共 N 条 + 第 1/3 页 + newest-first | API：page=1 默认 size=20 回显（B20/C1）；前端：Policy/JobLog 首屏测试（分页条三断言） |
| 1.2 跳页 | 整体替换非追加、高亮切换、滚回列表顶 | 前端测试：点第 2 页 → 新条目在/旧条目 `queryBy` 为 null + `aria-current` 切换 + `scrollIntoView` 断言 + 请求参数 page=2 |
| 1.3 每页条数切换 | 回第 1 页、第 1/1 页 | 前端测试：size=10/50 → page=1 重查、单页精简态「第 1/1 页」；API：size=50 合法（B19） |
| 1.4 切页失败 | 保留当前页 + 错误提示 + 重试成功 | 前端测试（两页各 1 条）：`加载第 2 页失败：服务异常` + 重试重发同页 page=2 + 显示态不前跳（指示仍第 1/3 页） |
| 1.5 Job 日志同款 + URL 预过滤 | 同组件基线、`#/job-logs?jobName=` 仍生效 | 前端测试：`initialJobName` 首查即带 jobName&page=1&size=20；App.tsx `key={jobName}` 重挂载链路代码核对（零改动） |
| 1.6 size 上限 | 前端只发 10/20/50；后端拒绝式防御 | 前端：选择器 options 恰为 10/20/50（组件测试）；后端：size=51 → 400/2001「size 超过上限 50」（B11/B12，拒绝不截断） |
| 2.1 改筛选重置页码 | 回第 1 页 + total 按筛选刷新 | 前端测试：行业/时间窗/关键词/状态/jobName 各 1 条（page=1 重查 + total 收缩断言）；源码：`loadFirst` 先 `setPage(1)` 再发查 |
| 2.2 筛后不足当前页 | 自动回退不空页 | 前端测试（两页各 1 条）：数据 45→21/30 收缩 → 请求序 page=3 → page=2 静默重发、指示「第 2 / 2 页」；源码：`fetchPolicies` 空页防御分支（`total>0 && page>1` → `ceil` 末页重发一次） |
| 2.3 筛后无结果 | 区分性空态 + 分页隐藏、无「第 1/0 页」 | 前端测试：关键词无命中「未找到包含「xx」的政策」+ CTA + `pagination-root` 为 null；组件测试：total=0 整条 return null |
| 2.4 在途竞态 | 旧响应不覆盖新结果 | 前端测试（两页各 1 条）：挂起翻页 + 切筛选 → 迟到响应含独有标记 id=999 → `queryBy(999)` 为 null、total 保持新值；源码：单点 AbortController + 回填前 `aborted` 检查 |
| 3.1 单状态筛选 | 只看失败 + total 刷新 + 页码重置 | API：status=FAILED total=7 == SQLite 真值 7（C8）；前端：状态分段 4 档切换测试（status=FAILED&page=1） |
| 3.2 jobName × 状态组合 | 交集结果 | API：PolicyTendencyJob×SUCCESS=69 == 真值、PolicyFetchJob×FAILED=0 == 真值（C8）；前端：组合一次请求断言 |
| 4.1 关键词命中 | 包含匹配 + total 刷新 + 页码重置 | API：「国务院」total=6 == 真值 6（C3）；前端：搜索输入→keyword&page=1 + total 收缩测试 |
| 4.2 无命中空态 | 区分性文案 + 分页隐藏 | 前端测试（同 2.3 关键词分支）；API：无命中 total=0 + 200（C3-miss） |
| 4.3 条件任意组合 | keyword×industry×days×page×size | API：days=30+新能源+「政策」total==真值、size=2 走页拼接 == 全量 id 序（C5）；前端：三条件一次请求测试 |
| 4.4 时间窗切换 | days 重新查询 + total 刷新 + 页码重置 | 前端：近 30 天分段切换测试（days=30&page=1）；API：days=7/30/90 计数 == 真值（C1） |

不在范围（PRD Won't，确认未做）：信息流分页化/类型筛选、订阅页分页化、倾向筛选、日期范围、分页状态进 URL——diff 核对 Feed/Subscriptions 零改动（§4）。

### 2.2 环境与方法

- 8080 为 HEAD（`0bc45ba`）真实运行进程；E2E 全部走 admin JWT 实调用。
- 计数正确性用 **SQLite 只读连接真值交叉验证**（与后端 `LocalDate.now(UTC).minusDays` 同口径复算 days 窗、`ESCAPE '\'` 同口径复算 keyword）；`job_execution_log` 为增长型表（测试窗口内 12,819→12,856，调度任务持续写入），计数断言采用**请求前后双快照夹逼**（db_before ≤ api_total ≤ db_after）+ 稳定子集（日频 Job）精确相等。
- 前端无浏览器工具：交互验收 = 组件/页面测试断言质量抽验（防恒真）+ 构建产物核对 + 源码逐条比对设计语义，三重证据（§3.3）。
- 深分页量级：12,856 行（较设计实测 12,385 增长 3.8%）。

## 3. 用例矩阵与结果

### 3.1 自动化回归（金字塔底座）

| 层 | 命令 | 结果 | M9 增量 |
| --- | --- | --- | --- |
| 后端全量 | `mvn verify` | **1356/1356**，BUILD SUCCESS（spotless 520 文件 clean、jacoco check 全过、ArchUnit 4 条过） | 8 个触及测试类 109 条全绿；交付区间后端测试方法**零删除**（diff 无 `-@Test`），1356 基线只增不减 |
| 前端单测 | `npx vitest run` | **278/278**（26 文件，4.23s），与开发自测基线持平 | `pagination.test.tsx` 14 条新增 + `Policy.test.tsx` 22 / `JobLog.test.tsx` 17 条——15 条旧「加载更多」用例按 PRD 风险表裁定成套重写为分页语义用例（净持平） |
| 前端 lint | `npx oxlint` | 0 errors / 27 warnings（存量，README M9 候选「oxlint 存量收敛」在册） | — |
| 前端构建 | `npm run build` | 成功（204ms，tsc 无错） | — |

### 3.2 接口级 E2E（8080，65/65 全过）

**A. 双模式共存（5 条）**：两接口游标模式响应形状 = 既有契约（`data` 仅 `policies/items + nextCursor`，无 `total/page/size` 混入）；同参重复请求 traceId 归一化后字节全等（确定性）；**页码 page=1 与游标首页同过滤同序逐 id 相等**（§3.5 回归锚点，政策 days=90 全量 14 条、Job PolicyTendencyJob 20 条双验证）。另补游标续翻 3 跳运行时验证（20+20+20 条 id 严格 DESC、零重复、nextCursor 链路完好）。

**B. 参数校验矩阵（27 条）**：page×cursor 互斥 400（两接口）；size/keyword/status 缺 page 400（msg 注明缺 page）；page=0/-1/1,000,001 400；size=0/51 400 **拒绝不截断**（两接口）；keyword 1 字符/65 字符 400（字段级 msg）；非法 status（RUNNING/小写 failed）400「未知 status: xxx」；size 非整数 400。合法边界：size=1/50/缺省 20、page 上限内、keyword 2/64 字符、keyword 与 status 纯空白 = 缺席不过滤、status=STARTED 合法；days=-5 clamp 7（==days7 计数）、days=9999 clamp 90（==days90 计数）。

**C. 组合过滤计数（21 条，SQLite 真值交叉）**：days=7/30/90 → 9/14/14 == 真值；行业 银行/白酒/不存在 → 2/2/0；关键词「国务院」6 == 真值、无命中 0；**通配符字面量**：keyword=`%%`/`__`/`\\` 均 0 命中 == 真值 0（若未转义会 14 全命中——转义生效的反证）；全组合 days30×新能源×「政策」truth=1 + size=2 走页拼接 == 全量 id 序；越界页 page=99 → **200 + 空列表 + total/page/size 如实回显**（ADR-0035 裁决语义）；政策 size=5 全量走页 14/14 严格 DESC 零缺零重；Job 单状态/组合 7 组 api_total 全落真值区间（FAILED=7 精确相等）；PolicyTendencyJob 走页 69/69 == 真值精确相等；page=999999 → 200 + 空列表。

**D. 深分页与性能（4 条）**：offset=11,980（size20）/11,950（size50）/组合过滤 offset=4,450 → 各返回满页、id 严格 DESC，耗时 **3.1 / 3.7 / 11.0 ms**（护栏线 P95≤50ms，实测最深 11ms 含组合过滤）；政策组合查询 2.6ms。

**E. 安全与鉴权（6 条）**：无 token 访问 `/policies`（游标+页码两模式）/`/job-logs`/`/feed/personal`/`/subscriptions` 全部 401；keyword 携带 `'; DROP TABLE policy_item;--` 按字面量处理（200 + total=0 + 库完好 14 行）。

### 3.3 前端交互三重证据（无浏览器工具，如实标注）

**证据一 · 测试断言质量抽验（防恒真）**：

- `pagination.test.tsx`：折叠纯函数对 UI 设计 §2.3 六组用例表逐一断言，另**穷举 8~60 页 × 全部当前页**断言数字按钮 ≤7、省略号 ≤2、首末页恒显（非采样）；受控语义（size 切换回调 50 但显示值仍 20）；total=0 → `pagination-root` 为 null；单页精简态无翻页按钮保留三件；首/末页 prev/next 原生 disabled；`aria-current="page"` 仅当前页；显示层 clamp（page=9/total=100 → 「第 5 / 5 页」）。
- `Policy.test.tsx` / `JobLog.test.tsx` 关键断言均为**双向断言**（目标态在 + 旧态 `queryBy` 为 null），非单侧存在性凑数：跳页整体替换、翻页失败显示态不前跳（指示仍第 1/3 页）+ 重试重发同页（末次请求 page=2）、空页防御回退（请求序 page=3 → page=2 两连发 + 指示「第 2 / 2 页」）、竞态用独有标记 id=999 迟到响应验证不覆盖。
- 弱断言清点：键盘可达用例仅断言 `toHaveBeenCalled()`（注释说明 Tab 序锚定），为可接受的下限证明——已记录，不构成缺陷。

**证据二 · 构建产物**：`dist/assets/index-*.js` 中 `pagination-total/page-indicator/size`、`policy-keyword-input`、`policy-clear-filters`、`job-log-clear-filters` 及模板前缀 `policy-days-`、`job-status-filter-`、`pagination-page-` 全量在场；**加载更多链路零残留**（`policy-load-more`/`job-log-load-more`/`*-more-error` 均 0 处）。

**证据三 · 源码逐条比对 UI 设计 §5/§7.1**：`listLoading`（骨架+筛选禁用+分页隐藏）与 `pageLoading`（数据保留+分页禁用+aria-busy+「加载中…」）两布尔分离；筛选变更先 `setPage(1)` 再发查；翻页失败目标页收进 `pageError` 重试闭包；空页防御 `total>0 && page>1` → `ceil(total/size)` 静默重发一次；单一 `listAbort` 中止点 + 回填前 `aborted` 检查；翻页/条数/筛选成功后 `scrollIntoView`；行业/jobName 选项全集仅无过滤第 1 页聚合；`initialJobName` 仅初始 state 不回写 URL（D9）；关键词输入值与提交值分离、trim<2 禁按钮不发请求、空提交=清关键词（D7）；`trackReadingOnce` 埋点原样保留。

### 3.4 覆盖率核查（jacoco，门禁外取数）

| M9 触及类 | 行覆盖 | 分支覆盖 |
| --- | --- | --- |
| PageQuery（新增） | 27/27 = **100%** | 20/20 = **100%** |
| PolicyController / JobLogController | 27/27、16/16 = **100%** | 17/18、10/10 |
| PolicyService / JobLogQueryService | 98% / **100%** | 18/22、7/10 |
| PolicyRepositoryImpl / JobExecutionLogRepositoryImpl | 99% / 96% | 41/56、34/48（含 M9 前既有方法的存量分支） |
| PolicyPagedView / JobLogPagedView | 100% | — |

增量行覆盖 ≥80% 门禁达标（实测 96~100%）；单测补位口径核验：英文大小写互命中/中文精确（`findPage_keyword_asciiCaseInsensitive_chineseExact`）、NULL summary 不命中、通配符字面量正例命中、likePattern 三字符与替换顺序——线上库当前无 ASCII 文本且 9/14 summary 为空，这三档仅单测可承载，断言真实非恒真（§7 R2）。

## 4. 回归红线（diff + 运行时双面证据）

| 红线项 | 证据 |
| --- | --- |
| Feed 无限滚动 | `Feed.tsx`/`Feed.test.tsx` 在 `5bd0e71^..0bc45ba` diff 清单外（clean）；`/feed/personal` 401 防线运行时 401 |
| 订阅页游标 | `Subscriptions.tsx`/`Subscriptions.test.tsx`/`api/subscription.ts` clean；`/subscriptions` 401 |
| 政策/Job 详情列 | `PolicyDetail.tsx`、`JobStatusBadge.tsx` clean；详情测试在两页测试内回归（30040 友好提示、倾向徽章 4 态） |
| 401 跳登录 | `api/http.ts` clean；JobLog 页测试含 401 清 token 跳 /login 用例；E2E 5 端点 401 |
| 阅读埋点 | `api/readingEvent.ts` clean；Policy.tsx `trackReadingOnce` 调用原样 |
| 游标模式字节级不变 | 三面证据：① Controller diff 仅新增分派，`page==null → listPolicies(days, industry, cursor)` 原样；② 响应形状 = 既有契约（无新字段混入）+ traceId 归一化字节全等；③ 运行时续翻 3 跳链路完好 + page=1 与游标首页逐 id 相等 |
| URL 预过滤入口 | `App.tsx` clean（`key={jobName}` + `initialJobName` 既有链路），页面测试断言首查即带 jobName |

## 5. 缺陷清单

| 编号 | 级别 | 摘要 | 定级依据与建议 |
| --- | --- | --- | --- |
| D1 | P3（文档） | [技术方案 §6 测试要点 4](../02-设计/技术方案-列表分页与查询条件.md) 写「mvn verify 1296 基线」，实际基线为 1356（M8 增量后） | 文档滞后引用，不影响行为；建议下次触碰该文档时勘误 |
| D2 | P3（文档） | 技术方案 §4.3 分页容器 testid 写 `pagination`，UI 设计 §6.2 与实现为 `pagination-root` | 双设计源不一致；实现从 UI 基线（正确取舍），建议技术方案同步勘误避免后续误用 |

无 P0/P1/P2 产品缺陷：契约行为面（校验/互斥/越界/计数/转义/深页/竞态/回退）在运行时与自动化两层均未见偏差。

## 6. 数据治理（测前/测后）

- 测前：`VACUUM INTO` 备份 `backend/data/.test-backups/pre-t66-20260922.db`（integrity_check=ok；policy_item=14、job_execution_log=12,819）。
- 测中：E2E 全部为只读 GET + 登录 POST；无任何写库请求（注入用例以库完好性反证）。
- 测后比对：`policy_item` 逐行（id/title/published_at）**零差异**；subject_master 5,226 / runtime_config 20 / watchlist_item 4 / reading_event 25 / prompt_template 5 计数全不变；`job_execution_log` 12,819→12,856（+37 全为 id > 12,819 的新增，备份内 id 零缺失——调度任务真实执行留痕，append-only 不清理）；integrity_check=ok。

## 7. 遗留风险

| # | 风险 | 说明 | 对策 |
| --- | --- | --- | --- |
| R1 | 前端无浏览器实走 | 本轮无浏览器工具，交互验收以「测试断言抽验 + 构建产物 + 源码比对」三重证据承载，视觉/焦点环/触控目标未做像素级走查 | 上线后首日人工过一遍两页（跳页/切条数/筛选联动/空态），异常走缺陷流程 |
| R2 | 三档口径仅单测承载 | 英文关键词大小写互命中、NULL summary 不命中、通配符字面量**正例**命中——线上库当前无 ASCII 文本、9/14 summary 为空、无字面通配符，E2E 无法构造（不写生产库）；E2E 已以「未转义即 14 全命中 vs 实际 0 命中」反证转义生效 | 单测（PolicyRepositoryImplTest 3 用例 + LikePatternTest 6 用例）持续守护；若后续接入英文源政策，E2E 补正例 |
| R3 | job_execution_log 无界增长 | 测试 30 分钟 +37 行（约 1.8 万/天量级外推），护栏线 50 万行 / P95 100ms 距离尚远（最深页 3.1ms、组合深页 11ms） | ADR-0035 护栏照旧；README M9 候选「job_execution_log 清理」建议提级排期 |
| R4 | oxlint 27 warnings 存量 | 非本轮引入（0 errors） | README backlog「oxlint 存量收敛」在册 |
| R5 | 5173 仅 IPv6 localhost 监听 | `127.0.0.1:5173` 拒连、`localhost:5173` 正常（Vite 默认行为） | 本机访问无影响，记录备案 |

## 8. Go/No-Go

**Go。**

- P0/P1 = 0（缺陷仅 2 条 P3 文档勘误），满足否决线清零要求。
- PRD 16 验收场景全部有证据承载（§2.1 对照表）：接口行为面在 8080 真实服务逐条实证且计数与 SQLite 真值全对齐；前端交互由三重证据承载（R1 方法限制如实标注，不构成否决项——断言质量抽验未发现恒真）。
- 自动化全绿（1356 + 278）且基线只增不减；覆盖率增量 96~100%（门禁 ≥80%）。
- 回归红线 diff 零改动 + 游标兼容三面证据闭合；消费流（信息流/订阅）零感知。
- 数据零破坏（§6）。
