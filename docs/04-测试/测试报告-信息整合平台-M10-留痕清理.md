# 🧪 测试报告 · 信息整合与 AI 分析平台 M10（留痕数据生命周期治理 T70~T74）

> 测试编号：TEST-20260925-M10｜ 测试人：测试工程师（虚拟团队）｜ 日期：2026-09-25
> 提测范围：REQ-20260925-07（4 故事 21 场景）全量交付 T70~T73——`1600283`（T70 删除端口）/`9018815`（T71 RETENTION_CLEANUP 收编）/`53c858c`（T72 窗口配置键与接口）+ 前端 `5a370bc`（T73 窗口分组）+ `7ea7c43`（spotless 对齐）
> 验收依据：[REQ-20260925-07-留痕数据生命周期治理.md](../01-需求/REQ-20260925-07-留痕数据生命周期治理.md)（21 场景逐条转化）+ [技术方案-留痕数据生命周期治理.md](../02-设计/技术方案-留痕数据生命周期治理.md) §4 契约/§6 测试要点 + [ADR-0036](../02-设计/adr/0036-留痕清理键空间与留痕明细载体.md)
> 代码基线：main @ `7ea7c43`（工作区仅 untracked `backend/.gitignore`，M7 起本地遗留，与本轮无关）
> 服务基线：8080 后端 HEAD 进程（RETENTION_CLEANUP 已注册、CRON `0 30 3 * * ?`、enabled=true；编排者已手动触发首轮 executionId=16334）+ admin 实账号 JWT

## 1. 结论先行

| 维度 | 结果 |
| --- | --- |
| 自动化全量（后端） | **1413/1413**（0 失败 0 错误 0 跳过，`mvn verify` BUILD SUCCESS 含 spotless/jacoco/ArchUnit 门禁；相对 M9 基线 1356 只增不减，其中 M10 触及 9 个测试类 **52 条**全绿；首轮跑出 1415/2 挂为 target 内**昨日陈旧 surefire 报告**（已删除的草稿类 `AnnounceRegistryRedTest`/`FinanceDiagTest`，见 §3.1 注），清空重跑得净 1413 全绿 |
| 自动化全量（前端） | **283/283**（26 文件，vitest run 5.55s）+ oxlint **0 errors**（27 warnings 存量不变）+ `vite build` 成功（468.38 kB / gzip 137.45 kB） |
| 接口级 E2E（8080 真实服务） | **22/22 全过**：GET 预填 1、2001 下限矩阵 8（四字段各一 + 0/负数/非整数字符串/缺失）、浮点/布尔探针 2、30065 与格式 2、鉴权 1、原值保留 1、合法改窗 roundtrip 3（14→2 生效 / 下限 7/2/35/35 / 改回默认）、手动触发 2、任务中心与 7 号键 3、Job 日志筛选 2 |
| 删除语义（核心红线） | **真实删除实证（一级证据）**：热改 `dataSourceEventDays` 14→2（无需重启）→ 手动触发 → **恰好 21 行**（预核对的受害者行集）被删、留痕明细 `data_source_event=21` 精确一致、其余三表/九张业务表零变化；改回 14 再触发 = 空轮 SUCCESS processed=0 且已删行不复活 |
| 四链路回归红线 | 概览 24h（llmToday/anomalyToday/policy24h 全等、windowRuns +1 为有机增长）/ 徽章（errors24h 全等、lastEventAt 仅前移）/ 成本 30d（8 项关键数字全等）/ 画像（reading_event 27 行零变化）——**清理零影响确认** |
| 通道锚点 | 六个既有 Job 最新 SUCCESS 行全部 (0,0,NULL)，全库 SUCCESS 行 error_message 非空者**仅** RetentionCleanupJob（通道非破坏性，真实库级锚点） |
| 覆盖率 | retention 域 9 个主类**行覆盖 100%**（jacoco 门禁外取数；增量 ≥80% 达标）；jacoco bundle 门禁 0.70 通过 |
| 缺陷 | **P3 × 3**（边界亚秒偏差 / INFO 摘要缺第四表窗口 / PATCH 非法类型错误码归一）；无 P0/P1/P2 |
| Go/No-Go | **Go**——P0/P1 = 0；21 条验收场景全部有运行时或自动化证据承载；测后数据全量恢复并逐表核对（§6） |

一句话：被测对象是「删数据的 Job」，本轮以测前 `VACUUM INTO` 备份 + 窗口热改到下限的方式在真实库完成了**真删实证**（21 行精确核对、白名单外表字节级不动、四链路前后零变化、删后回插复原）；三处 P3 均为契约/文案/错误码级瑕疵，不影响「永不全删」双防御与业务口径零影响两条红线。

## 2. 范围与环境

### 2.1 验收场景 → 证据来源（PRD 21 场景全对照）

| PRD 场景 | 验收要点 | 证据承载 | 证据等级 |
| --- | --- | --- | --- |
| 1.1 过期删、近期留 | 四表各自窗口独立判定 | E2E：dse=2 热改后 21 行过期行全删、≥cutoff 的 8181 行全留；jel(30)/llm(90)/read(90) 同轮各删 0（窗口内行保留）；单测 `runOnce_fourIndependentWindows` / `deleteExpiredBefore_eachOfFourTables` | **真实删除** + 单测 |
| 1.2 边界行不误删 | 严格早于、边界相等保留 | 单测 `deleteExpiredBefore_strictlyBeforeCutoff_boundaryRowKept`（整秒夹具通过）；**发现 D1**：真实 created_at 带亚秒精度，边界秒内的行字符串比较会失序（§5），本轮实删 21 行未命中边界秒、无实际误删 | 单测 + **契约偏差缺陷 D1** |
| 1.3 空轮 | SUCCESS + processed=0 | E2E：executionId=16396（改回 14 后）SUCCESS 0 + 四段全 0 明细；16334（编排者首轮）同为 0 | **真实运行** |
| 1.4 手动触发 | 同通道共守卫、立即执行 | E2E：POST /jobs/RETENTION_CLEANUP/run → 202 语义 + executionId=16390，与定时共用 JobExecutor CAS 通道 | **真实运行** |
| 1.5 首轮存量收敛 | 1.6 万行过期删 + 页面可用 | 现库最早行 09-21/22（部署太新，**无 >30d 行可删**）——真实大批量场景不可复现；由单测拆批（`runOnce_moreThanOneBatch` 循环至返回<limit + 计数准确）承载，21 行真删一轮 4ms 完成、期间页面请求正常 | **单测依据**（如实标注）+ 小批量真删 |
| 1.6 范围白名单 | 业务表行数不变 | E2E：真删轮前后 push_record/ai_brief/policy_item/anomaly_event/subject_master/prompt_template/runtime_config/watchlist/subscription_config 九表计数逐一全等 | **真实运行** |
| 2.1 合法改窗生效 | 改 7/2 下限也生效、无需重启 | E2E：14→2 保存后下一轮即按 2 天判定（真实删除 21 行）；7/2/35/35 下限值 PATCH roundtrip 全收 | **真实运行** |
| 2.2 改大不复活 | 不恢复、不报错 | E2E：改回 14 再触发 → processed=0、已删 21 行仍缺（复活数 0） | **真实运行** |
| 2.3 非法值拒绝 | PARAM_INVALID 字段级、原值保留 | E2E：四字段各低于下限 + 0/负数/缺失 → 2001 字段级 msg；失败后 GET 回读原值不变；**D3**：非整数字符串 → 50000、浮点 14.5 静默截断（§5） | **真实运行** + 缺陷 D3 |
| 2.4 缺省回退兜底 | 缺失/绕过写库回退默认 | 单测：`RetentionWindowsTest` 12 例（null 文档全默认/单字段缺失字段级回退/非法值回退不按 0/下限值采信）+ `runOnce_configKeyMissing` / `runOnce_illegalValueBypassingValidator`；**运行态不可注入实证**：快照型读路径（RuntimeConfigService volatile 快照，仅 write() 换新且必过校验），直接改库运行进程不可见——绕过路径在运行态本身不可达 | **单测依据**（如实标注） |
| 2.5 页面化编辑（Should） | 四字段编辑 + 校验提示 + 含义文案 | 前端 `TaskCenter.test.tsx` 5 例（GET 预填四窗口/文案含各表下限/低于下限前端拦截不发请求/双 PATCH 带 expectedUpdatedAt/PATCH 失败错误就地且 cron 不重放/GET 失败字段禁用）+ 283 全绿；后端两 PATCH 接口 E2E 实走；未做浏览器实走（同 M9 方法限制，如实标注） | 组件级 + 接口级 |
| 3.1 清理轮留痕 | SUCCESS + 合计 + 逐表明细 | E2E：16390 行 processed_count=21、error_message=`job_execution_log=0; data_source_event=21; llm_call_log=0; reading_event=0`（段序=枚举序） | **真实运行** |
| 3.2 任务中心可见 | 展示名/说明/CRON/启停 | E2E：7 Job 列表含 RETENTION_CLEANUP（留痕数据清理/CRON/enabled=true/nextExecutionTime=次日 03:30）；`job.RETENTION_CLEANUP` 走通用 job 域 PATCH 零特例（原值重写 200）；停用语义复用六 Job 共享通道（M5 已验） | **真实运行** |
| 3.3 Job 日志可筛 | jobName 筛选 + 分页/状态 | E2E：jobName=RetentionCleanupJob → total=3 三轮全出；+status=SUCCESS 组合 total=3；page/size 回显正常（M9 分页回归） | **真实运行** |
| 3.4 失败可感知 | FAILED + 不停摆 | 单测：`runOnce_singleTableFails_continuesOthers_throwsAtEndWithBothSides`（其余表照删 + 轮末异常含成功明细与失败原因）+ `runOnce_nextRoundAfterFailure_runsNormally` + Job 轮首重置 `run_resetsStatsAtRoundStart` | **单测依据**（真实库无故障注入面） |
| 4.1 概览 24h | 数字清理前后一致 | E2E：llmToday/anomalyToday/policy24h 逐字段全等；windowRuns 11497→11498（+1 为两快照间调度任务有机增长，无下降）；windowFailed 7 不变；七源徽章 errors24h 全等、lastEventAt 仅 QUOTE 前移（新事件到达） | **真实运行** |
| 4.2 成本报表 30d | 数字仍为 X | E2E：totalCalls/successCalls/failedCalls/cacheHits/prompt/completion/totalTokens/costMicros 八项**全等**（140/135/5/49/23139/138332/161471/576467） | **真实运行** |
| 4.3 推荐画像 | 30 天画像同口径 | E2E：reading_event 按 content_type 聚合 POLICY 16 + SUBJECT_DETAIL 11 清理前后零变化（90 天窗 ≥ 30 天画像窗）；未生成新推荐（避免真实 LLM 外呼消耗，见 §7 R4） | **真实运行**（数据面） |
| 4.4 Job 日志页 | 窗口内记录/排序/筛选不变 | E2E：jobName 筛选/状态组合/分页回显正常；jel 表 id ≤ 16386 计数连续无缺（本轮 jel 只增不删，唯一新增为清理轮自身留痕行） | **真实运行** |

不在范围（PRD Won't，确认未做）：push_record 纳入（真删轮 2 行不变）、业务表生命周期（九表计数不变）、软删/归档、独立指标端点、窗口按源细分——白名单实证承载。

### 2.2 环境与方法（数据库红线）

- **测前备份（红线）**：`VACUUM INTO 'data/.test-backups/pre-t74-20260925.db'`（6.4MB，含 WAL 合并），并快照全表行数与四表 MIN/MAX(created_at) 基线。
- **删除语义验证方式**：既有数据最早 09-21（无 >30d 行，30/14/90/90 默认窗下无可删行——首轮删 0 为正确行为）；本轮**热改 `dataSourceEventDays` 14→2**（API 窗口热改，测后已改回默认并在 §6 记录），制造 cutoff=now−2d 的真实可删集（预核对 21 行受害者清单），手动触发后逐行核对删除集合、他表零变化、业务表零变化；**测后从备份按原 id 回插 21 行**，MIN(created_at) 恢复至基线值。
- 增长型表处理：jel/dse 为持续写入表（调度任务每 10s~60s 留痕），计数断言采用「预核对受害者集合精确相等 + 保留集 ≥cutoff 精确断言 + id 连续性」而非裸计数对比。
- 8080 为 HEAD（`7ea7c43`）真实运行进程；E2E 全部走 admin JWT 实调用；无浏览器工具，前端交互以组件级测试 + 接口级实走承载（同 M9 三重证据精神的降档如实标注）。

## 3. 用例矩阵与结果

### 3.1 自动化回归（金字塔底座）

| 层 | 命令 | 结果 | M10 增量 |
| --- | --- | --- | --- |
| 后端全量 | `mvn verify` | **1413/1413**，BUILD SUCCESS（spotless clean、jacoco check 过、ArchUnit 过） | 9 个触及测试类 52 条全绿（ExpiredLogDeleterImpl 7 / RetentionCleanupService 8 / RetentionCleanupJob 3 / RetentionWindows 12 / RetentionConfigValidator 9 / RetentionConfigFacadeImpl 7 / RetentionController 4 / RetentionRuntimeConfigSeeder 2）；交付区间测试方法零删除，1356→1413 只增不减 |
| 前端单测 | `npx vitest run` | **283/283**（26 文件，5.55s） | `TaskCenter.test.tsx` +5（窗口分组用例，T73 修前红 5 失败修后绿）；278→283 |
| 前端 lint | `npx oxlint` | 0 errors / 27 warnings（存量，与 T73 自测一致） | — |
| 前端构建 | `npm run build` | 成功（227ms，tsc 无错） | — |

> 注（首轮 1415/2 挂的排查）：第一次 `mvn verify` 汇总 1415 条含 1 failure + 1 error，定位为 `target/surefire-reports` 内**昨日（09-24 23:54）陈旧报告**——`AnnounceRegistryRedTest`/`FinanceDiagTest` 两个开发自测草稿类，源码已删除、`git ls-files` 无此文件，报告时间为证。清空报告目录重跑得净 **1413/0/0**。教训已吸收：全量基线核对须以净目录报告为准。

### 3.2 接口级 E2E（8080，22/22 全过）

**A. 配置接口（12 条）**：GET 预填 windows=30/14/90/90 + limits min/default=7/30、2/14、35/90、35/90 + updatedAt；PATCH 低于下限矩阵（jel=6/dse=1/llm=34/read=34 各一条）→ 2001 字段级（含表名下限提示）；0 / 负数 / 字段缺失 → 2001（必填）；非法后 GET 回读原值不变；30065（过期 expectedUpdatedAt）/ 2001（expectedUpdatedAt 非 ISO-8601）；无 token 401。

**B. 窗口热改与触发（7 条）**：14→2 保存成功且 GET 回读一致；POST run → executionId；SUCCESS 留痕 processed=21 + 四段明细；受害者集 (<cutoff) 归零；他表计数不变；改回 14 再触发 → 空轮 0 + 不复活；下限值 7/2/35/35 roundtrip 全收后恢复默认。

**C. 任务中心与日志（3 条）**：7 Job 注册（RETENTION_CLEANUP 第七个，CRON/enabled）；`job.RETENTION_CLEANUP` 通用 job 域 PATCH 原值重写 200（零特例路径）；Job 日志 jobName 筛选 + 状态组合 + 分页回显（M9 回归）。

**D. 探针（2 条，转入缺陷 D3）**：`"14x"` / `true` → 50000（应 2001 字段级）；`14.5` → 静默截断为 14 采信（PRD「非整数拒绝」未达）。

### 3.3 通道锚点（真实库级）

| 检查 | 结果 |
| --- | --- |
| 六个既有 Job（SubjectSync/DailyRecommendation/PolicyTendency/AnomalyDetect/PushRetry/PolicyFetch）最新 SUCCESS 行 | 全部 (0,0,errorMessage=NULL)——通道扩展非破坏性 |
| 全库 SUCCESS 行 error_message 非空者 | **仅** RetentionCleanupJob（历史 0 条违例） |
| RETENTION_CLEANUP SUCCESS 行 | 恒四段明细、段序=枚举序（16334/16390/16396 三轮一致） |

### 3.4 覆盖率核查（jacoco，门禁外取数）

| 口径 | 结果 |
| --- | --- |
| M10 增量（retention 域 9 主类 + JobRunStats） | 行覆盖 **100%**（RetentionLogTable 15/15、RetentionConfigValidator 17/17、RetentionWindows 19/19、RetentionCleanupService 39/39、RetentionCleanupJob 15/15、ExpiredLogDeleterImpl 11/11、RetentionConfigFacadeImpl 33/33、Facade 4/4、Exception 2/2）——增量 ≥80% 达标 |
| 恒真断言抽验 | 明细格式/边界保留/字段级回退/失败续跑用例断言均有具体期望值与反向构造（如「不截断则边界行会被误删」注释对应的反证路径），未发现恒真凑数 |
| bundle 门禁 | pom `jacoco.min-coverage=0.70` check 通过（BUILD SUCCESS 组成部分） |

## 4. 回归红线（四链路前后对照）

| 链路 | 清理前 | 清理后 | 判定 |
| --- | --- | --- | --- |
| 概览 llmToday | 2544 / 8688 / OK | 全等 | 零变化 |
| 概览 anomalyToday / policy24h | 1 / 1（id=14） | 全等 | 零变化 |
| 概览 jobHealth | runs=11497 failed=7 | runs=11498 failed=7 | +1 为有机增长（两快照间调度任务新完成一轮），**无下降**=24h 窗口行未被删 |
| 七源徽章 | errors24h=20/35/5/0/54/43/0，七源 lastEventAt | errors24h 全等；仅 QUOTE lastEventAt 前移（新事件到达，时间更晚） | 零失真 |
| 成本 30d | 140/135/5/49/23139/138332/161471/576467 | **八项全等** | 零变化 |
| 画像（reading_event） | POLICY 16 + SUBJECT 11（MIN 09-23） | 全等 | 零变化 |
| Job 日志页 | M9 分页/筛选行为 | jobName/状态/分页回显正常；jel id 连续无缺 | 零变化 |

## 5. 缺陷清单

| 编号 | 级别 | 摘要 | 复现 / 证据 | 期望 | 实际 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| D1 | P3 | 边界秒亚秒行误删偏差：删除谓词 `created_at < cutoff(整秒截断)` 字符串比较，在边界秒 [cutoff, cutoff+1s) 内的真实行（created_at 带亚秒精度）字典序小于 cutoff 而被误判过期 | SQLite 证据：`'…T07:47:37.900Z' < '…T07:47:37Z'` = true；根因：ADR-0036/方案前提「四表 created_at 存整秒 ISO-8601 文本」与实际不符（实存 `Instant.toString()` 变长亚秒，如 `…T07:47:37.552745400Z`），单测夹具亦用整秒串故未拦截；本轮真删 21 行未命中边界秒（该秒内无行），概率约每轮 <0.05 行 | 边界语义严格「created_at 时刻早于 now−窗口」 | 实际边界为 `floor(now−窗口)+1s`（删多不足 1 秒内的行）；业务影响≈0（边界行本身已是窗口极限年龄，下游消费窗 ≥24h） | 新建（建议修复：行侧同宽截断 `substr(created_at,1,19) < ?` 或 cutoff 进位；修复须附整秒+亚秒双夹具回归用例） |
| D2 | P3 | 每轮 INFO 摘要日志缺 reading_event 窗口：输出「窗口 {jel}/{dse}/{llm} 天」三段 | 代码级证据 `RetentionCleanupService.runOnce` log.info 只取三字段（readingEventDays 未入参）；服务经 `mvn spring-boot:run` 控制台输出、无日志文件可截取，故按代码级定级 | 方案 §3.2「每轮 INFO 摘要（含窗口与耗时）」四表窗口齐载 | 只列 30/14/90 三段，reading_event 的 90 缺失——**判定为文案缺陷非设计如此**（方案明言含窗口，四表平权） | 新建（一行改动 + 断言四段格式） |
| D3 | P3 | PATCH /retention/windows 非法类型值错误码未归一：`"14x"`/`true` → 50000「服务异常」（Jackson 绑定异常直落兜底）；`14.5` → 静默截断为 14 采信 | E2E §3.2-D；校验器 `isIntegralNumber` 分支经此端点不可达（DTO 先绑定 Integer） | PRD 故事 2 场景 3：非整数 → 2001 PARAM_INVALID 字段级 | 字符串/布尔 50000；浮点被截断后过下限校验而收下（方向安全：截断值仍 ≥ 下限才入库，不会产生危险窗口，但违背「非整数拒绝」字面契约） | 新建（建议 DTO 层收 JsonNode 或加 HttpMessageNotReadableException 归一处理；对齐平台既有端点行为后统一处置） |

缺陷生命周期：三条均已开单（新建），P3 排期修复，修后须各附回归用例（D1 亚秒夹具 / D2 日志断言 / D3 类型矩阵）。

## 6. 数据治理（测前/测后）

| 项 | 内容 |
| --- | --- |
| 测前备份 | `backend/data/.test-backups/pre-t74-20260925.db`（VACUUM INTO，09-25 08:35） |
| 窗口改动记录 | `dataSourceEventDays`：14→2（E2E-12，09-25 00:37Z）→14（E2E-14 复原）；下限探针 7/2/35/35 roundtrip 后即恢复 30/14/90/90；**终态 GET 确认 = 默认值**，`job.RETENTION_CLEANUP` cron 原值重写后仍 `0 30 3 * * ?`/enabled=true |
| 删除与恢复 | 真删 21 行（executionId=16390）→ 测后从备份**按原 id 回插 21 行**，`created_at < cutoff` 计数恢复 21、dse MIN(created_at) 恢复 `2026-09-21T08:19:04Z`（=基线） |
| 逐表终态核对 | jel 16403（基线 16354，增量=测试窗口内调度任务有机写入+清理轮 3 行留痕，id 连续无缺）；dse 8203（基线 8197，+21 复原+有机增量）；llm 140 / read 27（与基线全等）；push 2 / ai 8 / policy 14 / anomaly 2 / subj 5226 / tpl 5 / rt 22 / wl 2 / subcfg 4（九表全等基线） |
| runtime_config | retention.global 与 job.RETENTION_CLEANUP 终值 = 种子默认（22 键总数不变） |

## 7. 遗留风险

| # | 风险 | 说明 | 对策 |
| --- | --- | --- | --- |
| R1 | 首轮大存量场景未真库复现 | 现库最早行 09-21，30 天窗口下无可删行，「1.6 万行 32 批」量级仅有单测拆批证据（21 行真删为单批） | 运行满 30 天后观察首轮 03:30 自动执行实际批数与耗时（与 REQ「按月抽查行数稳态」一并做） |
| R2 | CRON 自动触发路径未到点 | 测试窗口内无 03:30，自动触发与手动共用 JobExecutor 通道（六 Job 先例）但未直接观测 | 次日 03:30 后查 Job 日志页 RetentionCleanupJob 新 SUCCESS 行（INFO 摘要/明细可核对窗口与删除量） |
| R3 | 画像链路仅数据面验证 | 未生成新推荐（避免真实 LLM 外呼）；画像消费的 30 天窗行零变化已实证，生成侧口径未走 | 下次自然生成推荐时无异常即闭环 |
| R4 | D1 边界偏差待修 | 契约语义漂移虽小（<1s、方向多删边界秒），修复前每轮存在低概率边界误删 | P3 排期，修后以亚秒夹具回归用例锁死 |
| R5 | 停用语义未本轮实测 | 「停用后不再定时执行、手动仍可用」复用共享通道（M5 已验 + job 域键 PATCH 路径本轮验证） | 无需专项（共享语义），异常时走缺陷流程 |

## 8. Go/No-Go

**Go**。

- P0/P1 = 0（三条 P3 均不触及「永不全删」双防御、白名单、业务口径零影响三条红线）。
- 21 条验收场景全部落地：16 条真实运行证据 + 4 条单测依据如实标注（首轮存量/缺省回退/失败轮/页面化——其中页面化另有组件级与接口级双重证据）+ 1 条（边界行）单测通过但伴生契约偏差缺陷 D1。
- 自动化基线只增不减（1413 / 283），覆盖率增量 100%。
- 测后数据与配置全量恢复并逐表核对（§6），备份留档可复查。

发布后观察项（转入运维）：次日 03:30 自动轮留痕、按月四表行数稳态抽查（jel ≈11 万内 / dse 1.4~3 万 / llm 百级 / read 十级），持续增长即缺陷信号（REQ 护栏联动）。
